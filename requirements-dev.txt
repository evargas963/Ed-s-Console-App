# Dev / test dependencies (optional). Install: pip install -r requirements-dev.txt
#
# Native wheels are PINNED (2026-07-03) for CI reproducibility: a floating ">="
# numpy resolution segfaulted the CI runner inside matmul/argsort while the same
# content passed locally. Bump these deliberately and confirm CI stays green.
pytest>=8.0
pytest-xdist>=3.6.0
# RC-400: ruff is a REQUIRED dev dependency, not an optional convenience. The turn
# self-audit runs `python -m ruff check` as one of its four required checks, so without it
# the audit's verdict is FAIL rather than NOT_PROVEN and sixteen contract tests fail on a
# missing module instead of on anything they assert. It was absent from BOTH requirements
# files: the hardening workflow pip-installs it ad hoc and passed, while the pytest workflow
# and tools/bootstrap_worktree_venv.py install these files and did not. MEASURED on the
# runner, inside the audit's own record: "No module named ruff".
ruff>=0.16.3
numpy==2.3.5
pyyaml>=6.0
openpyxl>=3.1
tree-sitter==0.25.2
tree-sitter-typescript==0.23.2
tree-sitter-html==0.23.2
sentence-transformers>=2.6
