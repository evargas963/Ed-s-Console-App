---
description: Monday 2026-07-20 — confirm live collector runs new morning full-chain capture before counting GEX days
alwaysApply: true
---

# GEX Monday collector gate (2026-07-20)

Before treating forward `option_chain_morning_full` accrual as started:

1. Read and execute `reports/gex_r1_monday_collector_gate.md`.
2. Prove the live uvicorn/`server.py` process is up **and** started after the `maybe_persist_morning_full_chain` code landed.
3. After ~10:00 ET Monday, prove SQL rows for SPY/QQQ/IWM on `et_date='2026-07-20'`.
4. Write `reports/gex_r1_monday_collector_gate_result.json`. No PASS file → capture not started.

Do not equate “decisions were writing” with “morning full-chain code is live.”
