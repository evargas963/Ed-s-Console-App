# Claude Finish Adversarial Audit v28 — v27 Decide seal

**Target:** `60f40128` — v27 sealed: no slug speaks direction under !tradeable  
**Prior:** v27 REJECT (consolidated dirText LONG escape)  
**Auditor:** Cursor, 2026-07-29 (same-turn)

---

## Verdict: **ACCEPT — Decide pill mandate 100% sealed**

The v27 escape is closed end-to-end: code, locks (fire+quiet), and contract execution.  
I am willing to call the **Decide safety dimension 10/10 for the mandated surface** (horizon / ALL / PLAN trade-shaped paint under `!tradeable`).

| Claim | Result |
|---|---|
| consolidated+LONG+!tradeable → no LONG dirText | **ACCEPT** (node re-run) |
| sigDir / dirAttr neutralized under !tradeable | **ACCEPT** |
| CSS resurrectors deleted | **ACCEPT** |
| v27 escaped-shape fire fixture | **ACCEPT** |
| tradeable=true still paints | **ACCEPT** |
| Locks 5/5; suites 33 passed | **ACCEPT** (this turn) |
| code-health BLOCKING clean | **ACCEPT** (`--check` exit 0) |
| Absolute zero LONG anywhere in `#dr-hz-breakdown` | **OUT OF SEAL SCOPE** — see residual |

---

## Same-turn proof

```text
pytest tests/test_decide_direction_gate_v1.py → 5 passed
pytest + test_client_spot_single_faucet_v1 → 33 passed
code_health_panel.py --check → BLOCKING CLEAN

node contract:
  consolidated LONG false → dim, sigDir=neutral, dirText=NEUTRAL, dirAttr=NEUTRAL
  5c SHORT false → dim, dirText=—, sigDir=neutral, dirAttr=NEUTRAL
  5c LONG true → up, tf-glow-3, LONG (quiet control)
```

Live wire: `final_tradeable=False`, `final_bias=WAIT`, `operator_card_actionable=False`.

Helper law now:

```text
!tradeable → sigDir=neutral; dirText=NEUTRAL|—; dirAttr=NEUTRAL
(no !isConsolidated carve-out)
```

Paint site writes `visual.dirAttr` only; pre-v27 raw `dir` attribute write banned by lock.

---

## Residual (does not reopen the seal)

Inside collapsed `<details id="dr-hz-breakdown">`, when `cardTrust.trusted`, Bias/Eligibility can still print empirical / mhap `LONG|SHORT` text. That is **diagnostic breakdown**, not the tf-signal pill row. Not the v27 escape. Optional follow-up if you want zero directional vocabulary on the whole Decision card — not required to close RC-133/v27.

---

## Grade update

| Dimension | Was | Now |
|---|---:|---:|
| Decide safety | 3 | **10** (mandated pill/PLAN/ALL trade paint) |
| Overall institutional | ~7 | **~7.5** (Decide no longer the floor; LP-01 / RC-6 / F&P still cap) |

---

`CLAIM:` v27 seal ACCEPT; Decide pill mandate 100% · `DONE:` audit v28 · `NEXT:` LP-01 per operator · `BLOCKER:` none
