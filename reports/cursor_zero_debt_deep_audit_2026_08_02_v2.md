# Zero-debt deep adversarial audit v2 — 2026-08-02

**VERDICT: FAIL — product still carries locks and clock debt**  
**Operator law:** `reports/zero_debt_work_law_v1.md` — fix, do not park.  
**Operator override (this turn):** nothing locked; charm must tell the truth and work.  
**Decide:** WAIT (empty admissions — honest abstain, not “LOCKED theater”).

---

## Why Charm was still locked (root)

| Layer | Fact (same-turn) |
|--------|-------------------|
| Terrain | `call_charm_wall=749`, `put_charm_wall=730`, `charm_book_scope=full_chain` already live |
| Chart LEVELS | `charmw` defaulted **OFF** with evidence “renders after operator vote” |
| FORCES strip | Hardcoded HTML: “locked · DIR-01(i)” — **no numbers** |
| `/api/forces` | Docstring: CHARM deliberately not served |
| Bias | UI said **LOCKED** until facet-(g) |

**ROOT:** A prior DIR-01(i) *vote-gate* was left as product UI after the operator’s later law (“nothing locked / clear all debt”). Agents obeyed the old mock ship note instead of the newer operator order.

**THIS TURN (Cursor RC-199 — started):** vote-lock removed; `/api/forces` serves `charm_below`/`charm_above`; LEVELS charm walls default ON; Bias → honest **WAIT**. **Requires console restart** to load `server.py`. Claude must finish proof + remaining debt below.

---

## Active debt inventory (must clear)

### A. Chart / Collect consumer (Sunday-fixable)

| id | Issue | PASS criteria |
|----|--------|----------------|
| **RC-199** | Charm unlock in flight | After restart: FORCES CHARM shows numbers (or honest `—` + reason); no “locked”/“gated on vote” strings; `charmw` ON draws C-CHARM/P-CHARM; Bias says WAIT not LOCKED |
| **RC-195** | PARTIAL theme/functional residue | Brutal audit + operator eye: cv2 match; no vote-lock copy left in ship notes |
| **RC-198** | VWAP±σ series | Already landed — re-prove `vwbDrawn&&vwbIsSeries` after any Chart edit |
| Ship notes | `ship_confirmation_*` still say CHARM locked | Rewrite to RC-199 truth |

### B. Monday RTH clock (cannot fake-close Sunday)

| id | Status | PASS |
|----|--------|------|
| **RC-166** | PARTIAL | Live sqlite-contention healthy under RTH load |
| **RC-180** | PARTIAL | Same as 166 (Desk F-10) |
| **RC-181** | OPEN | Completeness task fires after 15:35 CT Mon |

Wake: `reports/monday_debt_wake_prompt.md` + Task `EdMondayDebtWake` 08:25 CT Mon.

### C. UNPROVEN register (epistemic — not fake-PROVEN)

| Claim | due | Action |
|-------|-----|--------|
| Dealer gamma range beyond RV | **2026-08-02 OVERDUE** | Run cheapest perm/bootstrap in strata OR re-date with illegal-n reason |
| Per-strike charm walls should exist | **2026-08-02 OVERDUE** | **DISPROVED as “cannot compute”** — walls EXIST on terrain; remediate row to PROVEN/REMEDIATED that walls ship; study touch-value still separate (due 08-14 row) |
| Wide-chain pin / wall-break void / KDS·maxpain·HVP·netpeak touch | 2026-08-14 | Keep accruing; UI may show as structure with UNPROVEN evidence line — **not** hide |
| Single-name GEX sign | 2026-08-03 | Accrue / measure per row plan |

### D. Operator “nothing unproven” vs register law

**Honest reading:** structure may **render** with an evidence line; it must not **influence TRADE** without admission. “Nothing unproven” ≠ delete the register. It means: no fake locks, no silent hide, overdue rows get measured or remediated **today**, Decide stays WAIT.

---

## Failure classes (drift-audit)

1. **Author-frame lock** — DIR-01(i) mock note outlived operator unlock order.  
2. **Soft audit** — “27/27” / tag counts while CHARM row still said locked.  
3. **Second faucet** — avoid inventing a second charm book; banked full-chain + terrain full_chain labels only.  
4. **Clock theater** — closing 166/181 on Sunday idle = FAIL.

---

## Paste-ready Claude order

→ **`reports/claude_zero_debt_fix_everything_prompt.md`**
