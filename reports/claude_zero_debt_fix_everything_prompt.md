# PASTE TO CLAUDE — FIX EVERYTHING (zero debt)

**OPERATOR GO — NON-NEGOTIABLE. No soft-stop. No LOCKED UI. No museum parks.**

Halt words only: `STOP` / `PAUSE` / `HANG IT UP` / `DO NOT CONTINUE`.

Read first: `AGENTS.md`, `reports/zero_debt_work_law_v1.md`, `reports/cursor_zero_debt_deep_audit_2026_08_02_v2.md`, `.claude/skills/drift-audit/SKILL.md` (execute), `governance/root_cause_log.md`, `governance/unproven_register.md`.

**Decide:** WAIT. Empty admissions. Do not invent TRADE influence.

---

## Why Charm was locked (do not re-litigate the vote)

Prior DIR-01(i) ship note gated FORCES CHARM and LEVELS `charmw` OFF. Operator **revoked** that: charm must be honest and work. Terrain already had walls (`call_charm_wall` / `put_charm_wall`, `charm_book_scope=full_chain`).

**Cursor started RC-199** (same day):  
- `server.get_forces` → `charm_below` / `charm_above` / `charm_book_scope=full_chain_banked`  
- Chart: CHARM row unlocked; `charmw` default ON; Bias → WAIT (not LOCKED)  
**You must:** restart console if needed, **prove** live numbers, finish any wiring bugs, close RC-199 with FIXED reach + tests, scrub every “locked · vote” string from Chart/ship notes.

---

## MUST FIX THIS TURN (Sunday-capable)

1. **Charm end-to-end**
   - Restart `:8000` from this worktree so `/api/forces` serves charm.
   - Prove SPY (then QQQ/IWM): `charm_below`/`charm_above` finite; Chart FORCES row paints both sides; no “locked”/“gated on vote”.
   - LEVELS Charm walls ON → C-CHARM / P-CHARM axis tags (or honest off-scale).
   - Book labels honest (full_chain vs single_expiry) — RC-184.
   - If chain unpack fails for charm: fix unpack to the same contract list `compute_exposures_by_strike` already uses — do not invent a second book.
   - Tests: forces payload includes charm fields; Chart source has no vote-lock literals for CHARM/Bias LOCKED.

2. **Zero LOCKED theater on Chart**
   - Grep `static/chart.html` + ship confirmations for `LOCKED`, `gated on vote`, `DIR-01(i)` charm gate — remove or rewrite to WAIT/honest absence.
   - Update `reports/ship_confirmation_static_chart_html_v6_1.md` (and any v6 ship notes) so they do not re-encode the vote-lock.

3. **Levels geometry honesty**
   - VWAP ±σ = **series curves** (RC-198) — do not regress to horizontal tips.
   - CALL/PUT RANGE = **dashed boxes**, no dulling fill (RC-197).
   - Re-run `node scratchpad/_v6_brutal_ui_audit.js` → 0 fail; `vwbDrawn && vwbIsSeries`.

4. **UNPROVEN overdue (due 2026-08-02)**
   - Dealer-gamma range beyond RV: run the cheapest significance test **or** honest re-date with why n illegal — no silent overdue.
   - “Per-strike charm walls should exist”: **remediate** — walls exist on terrain; update register (PROVEN/REMEDIATED with command). Touch-value study stays on the 08-14 row if still untested.
   - Do **not** fake PROVEN on 08-14 accrual rows.

5. **RC ledger**
   - Close RC-199 with FIXED / VISIBLE_SURFACE / END-TO-END / tests.
   - RC-195: close or leave PARTIAL only with named residue + clock.
   - Do not mass-fake-close RC-166/180/181.

---

## MONDAY ONLY (honest clock — prepare, do not fake)

| RC | When | Proof |
|----|------|-------|
| 166 / 180 | Mon RTH load | `/api/diagnostics/sqlite-contention` healthy |
| 181 | After 15:35 CT Mon | `EdRthCompletenessCheck` non-silent |

Use `reports/monday_debt_wake_prompt.md`. After Sunday work, those three may remain PARTIAL/OPEN with `NEXT_RTH_PROOF 2026-08-03` only.

---

## Forbidden

- Re-introducing charm/Bias LOCKED “until vote/study”.
- Soft-stop with “code ready, prove later” without UNBLOCKED-BY.
- Claiming Done while FORCES still says locked.
- Decide admissions / TRADE influence.
- Lock-harden arms race (operator: make it work).

---

## Done = same-turn evidence

```
CLAIM: …
DONE: RC-199 … 
REMAINING: only 166/180/181 Monday clocks + register rows with legal due dates
AUDIT: node scratchpad/_v6_brutal_ui_audit.js → N pass / 0 fail
FORCES: curl /api/forces?ticker=SPY → charm_below/above present
BLOCKER: none | …
```

No commit unless operator asks.
