{
  "calibration/backfill_outcomes.py:select_base": "SELECT outcome_1c, outcome_5c, outcome_15c, outcome_60c, outcome_1c_pts, outcome_5c_pts, outcome_15c_pts, outcome_60c_pts, ts_utc FROM snapshots WHERE ticker=? AND timeframe='1m'",
  "tests/test_issue16_normalized_outcome_materialize.py:snapshot_outcomes": "SELECT outcome_1c, outcome_15c, outcome_60c, outcome_15c_pts, outcome_60c_pts FROM snapshots WHERE ticker='SPY' AND timeframe=?",
  "tools/pin_neutral_reachability_audit_v1.py:pin_1m": "SELECT COUNT(*) FROM snapshots WHERE zone = 'pin_neutral' AND timeframe = ?",
  "tools/pin_neutral_reachability_audit_v1.py:pin_5m": "SELECT COUNT(*) FROM snapshots WHERE zone = 'pin_neutral' AND timeframe = '5m'",
  "tools/pin_neutral_reachability_audit_v1.py:n1m_total": "SELECT COUNT(*) FROM snapshots WHERE timeframe = ?",
  "tools/pin_neutral_reachability_audit_v1.py:pin_1m_null_g": "SELECT COUNT(*) FROM snapshots\nWHERE timeframe = ? AND zone = 'pin_neutral'\n  AND (net_gamma IS NULL OR net_delta IS NULL)",
  "tools/issue19_forward_canonical_validation_v1.py:pin_1m_since": "SELECT\n  COUNT(*) AS n_rows,\n  SUM(CASE WHEN outcome_filled = 1 THEN 1 ELSE 0 END) AS n_outcome_filled_1,\n  SUM(CASE WHEN outcome_1c IS NOT NULL THEN 1 ELSE 0 END) AS n_outcome_1c_set,\n  MIN(ts_utc) AS min_ts_utc,\n  MAX(ts_utc) AS max_ts_utc\nFROM snapshots\nWHERE zone = 'pin_neutral' AND timeframe = ? AND ts_utc >= ?",
  "tools/issue19_forward_canonical_validation_v1.py:pin_1m_rth_since": "SELECT\n  COUNT(*) AS n_rows,\n  SUM(CASE WHEN outcome_filled = 1 THEN 1 ELSE 0 END) AS n_outcome_filled_1,\n  SUM(CASE WHEN outcome_1c IS NOT NULL THEN 1 ELSE 0 END) AS n_outcome_1c_set\nFROM snapshots\nWHERE zone = 'pin_neutral' AND timeframe = ? AND market_session = 'rth'\n  AND ts_utc >= ?",
  "tools/issue19_forward_canonical_validation_v1.py:pin_5m_since": "SELECT COUNT(*) AS n_rows FROM snapshots\nWHERE zone = 'pin_neutral' AND timeframe = '5m' AND ts_utc >= ?",
  "tools/issue19_forward_canonical_validation_v1.py:funnel_count_base": "SELECT COUNT(*) AS n FROM snapshots\nWHERE zone = 'pin_neutral' AND timeframe = ? AND ts_utc >= ?",
  "tools/phase2_forward_write_verify.py:max_snapshot_id": "SELECT MAX(snapshot_id) FROM snapshots",
  "tools/phase2_forward_write_verify.py:post_cutoff_agg": "SELECT COUNT(*) AS n,\n       MIN(nearest_above_dist) AS nad_min,\n       MAX(nearest_above_dist) AS nad_max,\n       MIN(nearest_below_dist) AS nbd_min,\n       MAX(nearest_below_dist) AS nbd_max,\n       SUM(CASE WHEN nearest_above_dist IS NOT NULL AND nearest_above_dist < 0 THEN 1 ELSE 0 END) AS nad_neg,\n       SUM(CASE WHEN nearest_below_dist IS NOT NULL AND nearest_below_dist < 0 THEN 1 ELSE 0 END) AS nbd_neg\nFROM snapshots\nWHERE snapshot_id > ? AND timeframe = ?",
  "tools/phase2_forward_write_verify.py:legacy_nbd_neg": "SELECT COUNT(*) FROM snapshots WHERE snapshot_id <= ? AND timeframe = ? AND nearest_below_dist < 0",
  "tools/phase2_forward_write_verify.py:total_nbd_neg": "SELECT COUNT(*) FROM snapshots WHERE timeframe = ? AND nearest_below_dist < 0",
  "tools/pin_neutral_eligibility_funnel_v1.py:stage_total": "SELECT COUNT(*) FROM snapshots WHERE zone = 'pin_neutral'\n    AND timeframe IN (?, ?)\n    AND COALESCE(horizon_outcome_schema_version, ?) = ?",
  "tools/pin_neutral_eligibility_funnel_v1.py:stage_unfilled": "SELECT COUNT(*) FROM snapshots WHERE zone = 'pin_neutral'\n    AND timeframe IN (?, ?)\n    AND COALESCE(horizon_outcome_schema_version, ?) = ? AND COALESCE(outcome_filled,0)=0",
  "tools/pin_neutral_eligibility_funnel_v1.py:stage_filled": "SELECT COUNT(*) FROM snapshots WHERE zone = 'pin_neutral'\n    AND timeframe IN (?, ?)\n    AND COALESCE(horizon_outcome_schema_version, ?) = ? AND COALESCE(outcome_filled,0)=1",
  "tools/pin_neutral_eligibility_funnel_v1.py:stage_tf": "SELECT COUNT(*) FROM snapshots WHERE zone = 'pin_neutral'\n    AND timeframe IN (?, ?)\n    AND COALESCE(horizon_outcome_schema_version, ?) = ? AND timeframe = ?",
  "tools/pin_neutral_eligibility_funnel_v1.py:stage_tf_o1": "SELECT COUNT(*) FROM snapshots WHERE zone = 'pin_neutral'\n    AND timeframe IN (?, ?)\n    AND COALESCE(horizon_outcome_schema_version, ?) = ? AND timeframe = ? AND outcome_1c IS NOT NULL",
  "tools/pin_neutral_eligibility_funnel_v1.py:unfilled_no_anchor": "SELECT COUNT(*) FROM snapshots s\nWHERE s.zone = 'pin_neutral'\n  AND s.timeframe IN (?, ?)\n  AND COALESCE(s.horizon_outcome_schema_version, ?) = ?\n  AND COALESCE(s.outcome_filled, 0) = 0\n  AND NOT EXISTS (\n    SELECT 1 FROM price_bars_1m b\n    WHERE b.ticker = s.ticker AND b.bar_end_ts_utc <= s.ts_utc\n  )\n",
  "tools/pin_neutral_eligibility_funnel_v1.py:five_m_pin_o1": "SELECT COUNT(*) FROM snapshots WHERE zone = 'pin_neutral'\n    AND timeframe IN (?, ?)\n    AND COALESCE(horizon_outcome_schema_version, ?) = ? AND timeframe = ? AND outcome_1c IS NOT NULL",
  "tools/pin_neutral_eligibility_funnel_v1.py:spx_like": "SELECT COUNT(*) FROM snapshots WHERE zone = 'pin_neutral'\n    AND timeframe IN (?, ?)\n    AND COALESCE(horizon_outcome_schema_version, ?) = ? AND ticker LIKE '$SPX%'",
  "tools/pin_neutral_eligibility_funnel_v1.py:labeled_1m_spx": "SELECT COUNT(*) FROM snapshots WHERE ticker = ? AND timeframe = ? AND zone = 'pin_neutral' AND outcome_1c IS NOT NULL",
  "tools/pin_neutral_1m_5m_divergence_audit_v1.py:count_star": "SELECT COUNT(*) FROM snapshots",
  "tools/pin_neutral_1m_5m_divergence_audit_v1.py:by_ticker_agg": "SELECT ticker,\n       COUNT(*) AS n_total,\n       SUM(CASE WHEN COALESCE(outcome_filled,0)=1 THEN 1 ELSE 0 END) AS n_filled,\n       SUM(CASE WHEN outcome_1c IS NOT NULL THEN 1 ELSE 0 END) AS n_o1\nFROM snapshots",
  "tools/pin_neutral_1m_5m_divergence_audit_v1.py:unfilled_has_anchor": "SELECT COUNT(*) FROM snapshots s\nWHERE s.zone = 'pin_neutral'\n  AND s.timeframe = ?\n  AND COALESCE(s.horizon_outcome_schema_version, ?) = ?\n  AND COALESCE(s.outcome_filled,0)=0\n  AND EXISTS (\n  SELECT 1 FROM price_bars_1m b\n  WHERE b.ticker = s.ticker AND b.bar_end_ts_utc <= s.ts_utc\n)",
  "audit_training_data.py:rth_row_count": "SELECT COUNT(*) FROM snapshots WHERE (et_hour * 60 + et_minute) >= 570 AND (et_hour * 60 + et_minute) < 960 AND CAST(strftime('%w', substr(ts_et, 1, 10)) AS INTEGER) BETWEEN 1 AND 5 AND timeframe = ?",
  "audit_training_data.py:clean_rth_date_range": "SELECT MIN(substr(ts_et, 1, 10)), MAX(substr(ts_et, 1, 10)) FROM snapshots WHERE timeframe = ? AND NOT (dte < 0 OR (dte IS NULL AND expiry IS NOT NULL AND ts_et IS NOT NULL AND expiry < substr(ts_et, 1, 10))) AND (et_hour * 60 + et_minute) >= 570 AND (et_hour * 60 + et_minute) < 960 AND CAST(strftime('%w', substr(ts_et, 1, 10)) AS INTEGER) BETWEEN 1 AND 5",
  "audit_training_data.py:distinct_rth_days": "SELECT COUNT(DISTINCT substr(ts_et, 1, 10)) FROM snapshots WHERE timeframe = ? AND (et_hour * 60 + et_minute) >= 570 AND (et_hour * 60 + et_minute) < 960 AND CAST(strftime('%w', substr(ts_et, 1, 10)) AS INTEGER) BETWEEN 1 AND 5",
  "audit_gate_labels.py:rth_total": "SELECT COUNT(*) FROM snapshots WHERE (et_hour * 60 + et_minute) >= 570 AND (et_hour * 60 + et_minute) < 960 AND CAST(strftime('%w', substr(ts_et, 1, 10)) AS INTEGER) BETWEEN 1 AND 5 AND timeframe = ?",
  "calibration/audit_phase1.py:random_sample_ts": "SELECT ticker, ts_utc FROM snapshots\nWHERE timeframe=?\nORDER BY RANDOM() LIMIT 5000",
  "calibration/audit_phase1.py:structural_completeness": "SELECT\n  COUNT(*) AS n,\n  SUM(CASE WHEN zone IS NULL OR zone='' THEN 1 ELSE 0 END) AS zone_null,\n  SUM(CASE WHEN vwap_side IS NULL OR vwap_side='' THEN 1 ELSE 0 END) AS vwap_null,\n  SUM(CASE WHEN nearest_above_dist IS NOT NULL AND nearest_above_dist < -1e-9 THEN 1 ELSE 0 END) AS above_neg,\n  SUM(CASE WHEN nearest_below_dist IS NOT NULL AND nearest_below_dist < -1e-9 THEN 1 ELSE 0 END) AS below_neg\nFROM snapshots WHERE timeframe=?",
  "calibration/anchor_audit.py:base_population": "SELECT s.ticker, s.ts_utc, s.market_session\nFROM snapshots s\nWHERE s.timeframe = ?",
  "backfill_flow_imbalance.py:select_candidates": "SELECT snapshot_id, spot, option_chain_json, flow_imbalance\nFROM snapshots\nWHERE timeframe IN (?, ?)\n  AND option_chain_json IS NOT NULL\n  AND length(option_chain_json) > 15",
  "tools/pin_neutral_anchor_feasibility_sample_v1.py:sample_oldest": "SELECT snapshot_id, ticker, ts_utc, timeframe\nFROM snapshots\nWHERE zone='pin_neutral' AND outcome_filled=0\n  AND timeframe IN (?, ?)\nORDER BY ts_utc ASC\nLIMIT 1",
  "tools/pin_neutral_anchor_feasibility_sample_v1.py:sample_newest": "SELECT snapshot_id, ticker, ts_utc, timeframe\nFROM snapshots\nWHERE zone='pin_neutral' AND outcome_filled=0\n  AND timeframe IN (?, ?)\nORDER BY ts_utc DESC\nLIMIT 1",
  "tools/rth_pin_neutral_health_probe_v1.py:pin_1m_14d": "SELECT\n  COUNT(*) AS n,\n  SUM(CASE WHEN outcome_filled = 1 THEN 1 ELSE 0 END) AS n_filled,\n  SUM(CASE WHEN outcome_1c IS NOT NULL THEN 1 ELSE 0 END) AS n_outcome_1c\nFROM snapshots\nWHERE zone = 'pin_neutral' AND timeframe = '1m' AND ts_utc >= ?",
  "tools/rth_pin_neutral_health_probe_v1.py:pin_1m_rth_14d": "SELECT\n  COUNT(*) AS n,\n  SUM(CASE WHEN outcome_filled = 1 THEN 1 ELSE 0 END) AS n_filled,\n  SUM(CASE WHEN outcome_1c IS NOT NULL THEN 1 ELSE 0 END) AS n_outcome_1c\nFROM snapshots\nWHERE zone = 'pin_neutral' AND timeframe = '1m' AND market_session = 'rth'\n  AND ts_utc >= ?",
  "tools/rth_pin_neutral_health_probe_v1.py:pin_5m_14d": "SELECT\n  COUNT(*) AS n,\n  SUM(CASE WHEN outcome_filled = 1 THEN 1 ELSE 0 END) AS n_filled\nFROM snapshots\nWHERE zone = 'pin_neutral' AND timeframe = '5m' AND ts_utc >= ?",
  "tools/_diag_pin_neutral_outcomes.py:pin_tf_summary": "SELECT COUNT(*) n, MIN(ts_utc) tmin, MAX(ts_utc) tmax FROM snapshots WHERE zone='pin_neutral' AND timeframe IN (?, ?)",
  "tools/_diag_pin_neutral_outcomes.py:pin_tf_window": "SELECT COUNT(*) n FROM snapshots WHERE zone='pin_neutral' AND timeframe IN (?, ?) AND ts_utc > ? AND ts_utc < ?",
  "tools/_diag_pin_neutral_outcomes.py:schema_v_hist": "SELECT COALESCE(horizon_outcome_schema_version, -1) v, COUNT(*) n FROM snapshots WHERE zone='pin_neutral' AND timeframe IN (?, ?) GROUP BY v ORDER BY n DESC",
  "tools/_diag_pin_neutral_outcomes.py:outcome_filled_hist": "SELECT outcome_filled, COUNT(*) n FROM snapshots WHERE zone='pin_neutral' AND timeframe IN (?, ?) GROUP BY outcome_filled",
  "tools/_diag_pin_neutral_outcomes.py:outcome_1c_hist": "SELECT outcome_1c IS NOT NULL AS has_o1, COUNT(*) n FROM snapshots WHERE zone='pin_neutral' AND timeframe IN (?, ?) GROUP BY has_o1",
  "tools/_diag_pin_neutral_outcomes.py:ticker_hist": "SELECT ticker, COUNT(*) n FROM snapshots WHERE zone='pin_neutral' AND timeframe IN (?, ?) GROUP BY ticker ORDER BY n DESC",
  "tools/_diag_pin_neutral_outcomes.py:pin_bull_schema_sample": "SELECT COALESCE(horizon_outcome_schema_version,-1) v, COUNT(*) n FROM snapshots WHERE zone='pin_bull' AND timeframe=? GROUP BY v ORDER BY n DESC LIMIT 5",
  "verification/similarity_feature_audit.py:tier1_ablation": "SELECT *, 1 AS match_tier FROM snapshots\nWHERE ticker = ? AND timeframe = ? AND zone = ? AND vwap_side = ?\n  AND outcome_1c IS NOT NULL\n  AND ((nearest_above_dist IS NULL AND ? IS NULL) OR (nearest_above_dist BETWEEN ? AND ?))\n  AND ((nearest_below_dist IS NULL AND ? IS NULL) OR (nearest_below_dist BETWEEN ? AND ?))\n",
  "verification/similarity_feature_audit.py:tier2_ablation": "SELECT *, 2 AS match_tier FROM snapshots\nWHERE ticker = ? AND timeframe = ? AND zone = ? AND vwap_side = ?\n  AND outcome_1c IS NOT NULL\n  AND ((nearest_above_dist IS NULL AND ? IS NULL) OR (nearest_above_dist BETWEEN ? AND ?))\n",
  "verification/similarity_feature_audit.py:ortho_below_only": "SELECT *, 91 AS match_tier FROM snapshots\nWHERE ticker = ? AND timeframe = ? AND zone = ? AND vwap_side = ?\n  AND outcome_1c IS NOT NULL\n  AND ((nearest_below_dist IS NULL AND ? IS NULL) OR (nearest_below_dist BETWEEN ? AND ?))\n",
  "verification/similarity_feature_audit.py:ortho_drop_zone": "SELECT *, 92 AS match_tier FROM snapshots\nWHERE ticker = ? AND timeframe = ? AND vwap_side = ?\n  AND outcome_1c IS NOT NULL\n  AND ((nearest_above_dist IS NULL AND ? IS NULL) OR (nearest_above_dist BETWEEN ? AND ?))\n  AND ((nearest_below_dist IS NULL AND ? IS NULL) OR (nearest_below_dist BETWEEN ? AND ?))\n",
  "verification/similarity_feature_audit.py:ortho_drop_vwap": "SELECT *, 93 AS match_tier FROM snapshots\nWHERE ticker = ? AND timeframe = ? AND zone = ?\n  AND outcome_1c IS NOT NULL\n  AND ((nearest_above_dist IS NULL AND ? IS NULL) OR (nearest_above_dist BETWEEN ? AND ?))\n  AND ((nearest_below_dist IS NULL AND ? IS NULL) OR (nearest_below_dist BETWEEN ? AND ?))\n"
}
