# Vendor path registry — SCHWAB_UNIVERSAL_COVERAGE_PROGRAM_V2 § G1.1
# Authority: gatekeeper review required for any add/change/remove (same bar as contract).
# Each entry: path prefix (POSIX), provenance, contract_clause.
# Paths are normalized with trailing slashes stripped; matching is prefix + "/".

version: 1
entries:
  - path: "node_modules/"
    provenance: "npm ecosystem; lockfile package-lock.json / package.json"
    contract_clause: "G1.1"
