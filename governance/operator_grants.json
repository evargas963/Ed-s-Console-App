{
  "_doc": "Operator standing grants (RC-360). A grant is ACTIVE only when this file is COMMITTED at HEAD — operator_law_guard reads it via `git show HEAD:governance/operator_grants.json`, never the worktree, so an agent cannot self-grant: the enabling commit is operator-lane and IS the ratification. Revoke by committing granted:false.",
  "grants": {
    "claude_no_verify_checkpoints": {
      "granted": false,
      "granted_on": "2026-08-16",
      "revoked_on": "2026-08-16",
      "revoked_reason": "RC-389: --no-verify hid five_why 0->4 inside standing red. Commits use the delta gate (precommit_institutional -> check_delta_adds_no_debt --staged). Re-granting without that bind fails check_no_verify_cannot_hide_delta.",
      "operator_quote": "this can never happen again",
      "scope": "REVOKED. Checkpoint path is a normal commit after python tools/check_delta_adds_no_debt.py --base origin/main --staged PASSES. --no-verify is operator-only.",
      "revocation": "commit this entry with granted:false"
    }
  }
}
