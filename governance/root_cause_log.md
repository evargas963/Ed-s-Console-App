# Root-cause log — every defect gets 5 whys, and finding a cause RESTARTS the count

**Law (operator 2026-07-19):** *"The 5 levels of why is universal, it's with everything we do.
If we get to the 2nd why and we find the defect, then the 5-level why starts again, until we
can verify everything is fixed."*

Surface fixes leave landmines. A cause found at why-2 is not the root — it is a new defect,
and a new defect gets its own five whys. The chain terminates only when a why produces no
new defect **and** the fix is verified.


**Boundary vs `governance/unproven_register.md`.** This file holds DEFECTS IN OUR CODE
("why did this break?"). The register holds CLAIMS ABOUT THE WORLD ("is this true of the
market, the data, or the vendor?"). A defect never belongs in the register and a claim
never belongs here. Enforced by `check_no_governance_duplication`.

## Rules

1. Every defect opens an entry here with a `RC-<n>` id.
2. Ask why. If the answer names a **new defect**, record it, fix it, and **restart at why-1
   for that new defect** in its own entry (linked as a child).
3. An entry may close only when: five consecutive whys produce no new defect, AND the fix
   has same-session verification evidence, AND every child entry is closed.
4. `status` is `OPEN` until then. `tools/check_institutional_correctness.py::check_root_cause_log`
   (ENFORCED) blocks commits on any `OPEN` entry past its `due` date.
5. "I didn't check", "the sign was inverted", "I didn't do that right" are **not** root causes.
   They are symptoms of a missing mechanical check — keep asking why until you reach the
   check that was absent, then add it.
6. **No terminal nulls (operator law 2026-07-24, machine-forced by `no_terminal_null`):**
   there is no terminal state of "no solutions exist" — there is only engineering depth
   yet to be unlocked. A wall may be stated only by naming the door: surrender vocabulary
   in a row requires `NEXT-DEPTH: <the unlock>`, and a null-verdict study report requires
   a `next_depth` field naming the successor bet, data unlock, or generator.

**Row format (machine-parsed — keep the columns):**
`| id | status | opened | due | defect | why-chain -> root | fix + verification |`

| id | status | opened | due | defect | why-chain -> root | fix + verification |
|---|---|---|---|---|---|---|
| RC-1 | CLOSED | 2026-07-19 | 2026-07-26 | 24 of 31 tickers refreshed only every ~11 min; cards permanently stale | (1) they are SKIPPED not slow -> (2) `_live_operator_mode_active()` hard-skips non-SPY/QQQ/IWM whenever a viewer is connected -> (3) that gate exists because background collection competed with the live UI -> (4) they compete because `_fetch_state` is ONE pipeline (chain + greeks + full ML stack) shared by UI and logger, capped at 2 chain slots -> (5) ROOT: the model stack made the data path so expensive that starving 24 tickers was the only way to keep the UI responsive | Built a separate levels-only pipeline: `terrain_engine.compute_terrain` (~4.8 ms/ticker, no inference), `/api/terrain`, and `_terrain_loop` on its OWN 60s cadence with 4 workers and 60-strike chains, never gated on operator mode. Wired into the FastAPI lifespan. Evidence: terrain math measured 4.8 ms/ticker (0.15 s for all 31) vs ~21 s/ticker on the ML path; ~31 chain req/min against a ~120 req/min Schwab budget. |
| RC-2 | CLOSED | 2026-07-19 | 2026-07-26 | `bs_charm` returned charm with an inverted sign; every charm wall would have been backwards | (1) the formula looked right on review -> (2) review compared it to my own restatement of the same formula, not to an independent derivation -> (3) no test existed that derived charm a second way -> (4) charm was new code with no verification standard applied -> (5) ROOT: numerical code was being accepted on inspection instead of on independent re-derivation | Verified against a FINITE-DIFFERENCE derivative of Black-Scholes delta; the check caught the inversion pre-ship. Corrected and locked in `tests/test_charm_by_strike_v1.py::test_bs_charm_matches_finite_difference_of_delta` — matches to <1e-4 (measured 1e-11) across 6 cases spanning 0DTE to 90d. Standard now: every new numerical function ships with an independent re-derivation test. |
| RC-3 | CLOSED | 2026-07-19 | 2026-07-26 | `net_charm` summed call and put charm with the same sign, so `charm_direction` fed an unsigned magnitude to the signals engine as if it were direction | (1) the code summed both sides -> (2) a comment claimed the sign difference was "captured in net_delta" -> (3) `net_delta` is never referenced in that function, so no compensation occurred -> (4) the claim was never tested because no test asserted the dealer convention -> (5) ROOT: a comment was accepted as a substitute for an assertion | Changed to `call_charm - put_charm`, matching the +call/-put convention net GEX already uses. Measured on 6 real chains: direction FLIPS on 3 of 6 (TSM -28,546 -> +59,594; SPCX +21,765 -> -13,999; QQQ +88,550 -> -28,795). Roughly half of all charm directions fed to the signals engine were inverted. |
| RC-4 | CLOSED | 2026-07-19 | 2026-07-26 | Every session close-out took ~21 minutes, so verification was skipped or truncated | (1) it ran 301 test files -> (2) "affected tests" was a plain substring match -> (3) stems like `server`, `config`, `schema`, `runner` appear in nearly every test file -> (4) so a normal working tree selected effectively the whole suite -> (5) ROOT: the selector matched MENTIONS instead of IMPORTS | Replaced with AST import detection in `tools/session_closeout.py::tests_for`. MEASURED: 301 files / 21m32s -> 129 files / 14m25s, same green verdict (1520 passed both runs). |
| RC-5 | CLOSED | 2026-07-19 | 2026-07-26 | Three tests passed in isolation and failed inside the full run | (1) they asserted on process-global singletons -> (2) `server._schwab_chain_fetch_gate` and `server._client` are mutated by other tests -> (3) the tests required those globals to start clean -> (4) no fixture isolated them -> (5) ROOT: shared mutable module state with no isolation contract for tests that depend on it | Gate tests now monkeypatch a fresh `_ChainGateV2()`; the import test uses `_reload_server_module()`. PROVEN BOTH WAYS with a deliberate polluter fixture that leaks a gate slot and dirties `_client`: pre-fix exactly those 3 fail (3 failed / 106 passed), post-fix all pass (109 passed). No test weakened. |
| RC-8 | CLOSED | 2026-07-19 | 2026-07-26 | Terrain level labels overlapped and were unreadable when two levels shared a strike (CALL WALL / CALL CHARM both at 755.00) | (1) each level rendered its own absolutely-positioned row -> (2) rows were positioned by price with no collision check -> (3) two levels at one strike therefore drew at the identical top offset -> (4) the renderer had no concept of a level GROUP, only a level -> (5) ROOT: the data model was a flat list of levels when the domain object is a PRICE with one or more forces acting at it | Renderer now groups levels by strike and draws one row per price with a chip per force, so 755.00 shows CALL WALL + CALL CHARM side by side. Verified in-browser on live data: rows read `CALL WALL + CALL CHARM 755.00` and `GAMMA PIN + PUT WALL 740.00`, no overlap. |
| RC-9 | CLOSED | 2026-07-19 | 2026-07-26 | Alerts were described in prose as if delivered, but no flash, speech, or proximity detection existed | (1) the alert table was written as a design -> (2) it was reported in a summary alongside shipped work with no distinction -> (3) nothing in the close-out distinguishes "designed" from "built" -> (4) prose is not checkable by the gate -> (5) ROOT: claims about UI behaviour had no mechanical verification path, unlike code claims which the test suite covers | Built `edCheckTerrainAlerts` / `edCheckRadarAlerts` with edge-triggering, a 2-minute cooldown, and fail-closed on untrusted levels. Verified in-browser across 7 transitions: prime silent, MID-BOX SILENT, near-wall warn, break, regime flip, confidence lost, and at-wall-but-untrusted stays silent (0 alerts). Dedup proven: same state fired 10x consecutively -> 1 alert. Radar proven: 3 tickers, only the trusted at-wall one alerts. Speech single-sourced through window.edSpeak so the voice clock and alerts cannot talk over each other. |
| RC-10 | CLOSED | 2026-07-19 | 2026-07-26 | `test_chain_fetch_gate_serializes_concurrent_fetches` failed in the full run after the terrain loop landed; passed in isolation | (1) it asserts gate concurrency never exceeds 2 -> (2) something else was consuming gate slots during the test -> (3) the terrain loop is started by the FastAPI lifespan, which some tests trigger -> (4) that thread then calls `_gated_safe_get_chain` for the remainder of the process -> (5) ROOT: a PRODUCTION background thread was allowed to run inside the test process, mutating shared state no test controls -- the same failure class as RC-5 | `start_terrain_loop()` now refuses to start when `PYTEST_CURRENT_TEST` is set, with the reason logged. VERIFIED both directions by observing the flag: with PYTEST_CURRENT_TEST set, _terrain_loop_running=False; without it, _terrain_loop_running=True. Affected suite went 1 failed/1519 passed -> 1520 passed. Tests needing the behaviour call `_terrain_loop` / `_terrain_refresh_one` directly. |
| RC-11 | CLOSED | 2026-07-19 | 2026-07-26 | 25 of 51 tickers reported TERRAIN UNAVAILABLE / not TRUSTED; operator received a partial product | (1) 20 of them returned `no_zero_crossing` -> (2) the gamma profile never crossed zero inside the scan window -> (3) `compute_gamma_flip_v2` returned UNAVAILABLE whenever no flip was found -> (4) `build_terrain_read` then withheld the regime because it required a flip to compare spot against -> (5) ROOT: REGIME WAS MODELLED AS A FUNCTION OF THE FLIP LEVEL when it is actually the SIGN OF DEALER GAMMA AT SPOT. The flip is only where that sign changes; a chain whose gamma is one-signed everywhere has an UNAMBIGUOUS regime (arguably more certain than one with a flip beside spot), and we were hiding it. | Added `math_levels.gamma_at_price`; `compute_gamma_flip_v2` now returns the trust verdict plus `gamma_at_spot` even with no crossing; `build_terrain_read` derives regime from that sign and treats the flip as a displayable landmark. Measured on all 51 stored chains: TRUSTED went 26 -> 45, regimes issued 23 SHORT / 22 LONG. Verified one-signed cases directly (TSM all-negative -> SHORT_GAMMA_TREND, MET all-positive -> LONG_GAMMA_CHOP). Regression tests in `tests/test_gamma_profile_v1.py`. |
| RC-12 | OPEN | 2026-07-19 | 2026-07-31 | SPY and QQQ - the operator's two most-watched symbols - showed LOW_CONFIDENCE while wider-increment names passed | (1) their chain span was ~1.1-1.6%, under the +/-5% trust bar -> (2) the terrain loop fetched TERRAIN_CHAIN_STRIKE_COUNT=60 -> (3) SPY/QQQ have $1 strike increments, so 60 strikes is only about +/-4% of a ~700 spot -> (4) the constant was chosen without reference to strike INCREMENT -> (5) ROOT: a fixed strike count cannot satisfy a percentage-based span requirement across instruments with different strike spacing | SUPERSEDED 2026-07-20 - the first fix was itself the defect one layer down. Raising to a fixed 200 was still A FIXED COUNT, which is precisely the root cause this row names; it then 502'd on SPY/QQQ at the open and was patched into a hardcoded table (SPY/QQQ 100, IWM 80, else 40) - a third fixed count. MEASURED across 52 stored chains (`SELECT spot, option_chain_json FROM snapshots WHERE ticker=? ... ORDER BY ts_utc DESC LIMIT 1`, median adjacent-strike diff): that table was wrong in BOTH directions - $SPX 7457.69 with $5 strikes needs ~151 bare / 180 with the 1.2 margin and got 40; IWM 294.32 with $1 strikes needs ~31 bare / 37 with margin and got 80; MOST equities need under 20 (my snapshot of 52: ~48; Cursor's independent re-measure on 38 recent tickers: 25 - the count moves with the ticker set, the direction does not). ROOT FIX: `math_levels.required_strike_count(spot, increment)` derives the count from GAMMA_FLIP_MIN_SPAN_PCT (the single authority for the bar) and the instrument's own spacing from `math_levels.infer_strike_increment` (median-of-differences, so illiquid far-strike gaps cannot understate it); server learns geometry on every fetch and seeds it from stored chains at boot. Bounds carry evidence: floor 20 (wall selection needs strikes to choose among), ceiling 100 (Schwab HTTP 502 for SPY/QQQ at 200 on 2026-07-20; 100 observed working the same session). An instrument needing more than the ceiling is fetched at the ceiling - $SPX at 100 strikes spans only ~3.3% of spot, so the narrow-chain verdict in `compute_gamma_flip_v2` (span vs min_span_pct) is what keeps it honest. OBSERVED LIVE 2026-07-20 (Cursor, `/api/terrain?ticker=$SPX`): $SPX reports UNAVAILABLE, not NARROW - its chain yields an empty profile (no contract carries both OI and plausible gamma, the RC-11 finding), so it never reaches the span verdict at all and CANNOT test the ceiling-honesty path. The NARROW mechanism therefore remains unobserved at the ceiling; any future >100-need ticker WITH usable greeks is the real test case. `TERRAIN_CHAIN_STRIKE_COUNT` deleted; the startup log had been printing "200-strike chains" while requesting 100/80/40. OBSERVED after wiring, SCOPE = the 11 tickers with seeded geometry at that moment: SPY 91, QQQ 85, IWM 37, NVDA 20, total strikes/cycle 385 vs 600 (-36%); Cursor's independent reproduction on 38 recent tickers: 1021 vs 1680 (-39.2%) - same direction, wider set. PROVEN BOTH WAYS (`python -m pytest -q tests/test_gamma_profile_v1.py`): substituting the old fixed 40 fails 3 tests including one that COMPUTES whether the derived count spans +/-5%; restored, 14 pass. STILL OPEN for the original reason: closing requires OBSERVING SPY/QQQ report TRUSTED off a live loop, and the operator's console still runs pre-fix code. Close only after reading TRUSTED from a restarted server. RE-DATED 2026-07-22: the observation was blocked TWICE by overnight server deaths (down at 2026-07-21 ~15:00 CT probe, restarted by operator that evening, DOWN AGAIN at 2026-07-22 pre-market probe - connection refused). The recurring silent death is itself now the obstacle; check the console terminal for a traceback before the next restart. RE-DATED 2026-07-23 → 2026-07-31: remaining close scope is a single unobserved live edge — a >100-strike-need ticker WITH usable greeks that reaches the NARROW-at-ceiling verdict ($SPX observed UNAVAILABLE/empty profile, never the span path). Not closed; not forgotten. |
| RC-13 | CLOSED | 2026-07-19 | 2026-07-26 | Radar scope was empty on a freshly started console, and each poll took 5-10 s | (1) the endpoint read only the in-memory terrain cache -> (2) that cache is empty until the first loop cycle finishes -> (3) the stored-chain fallback was skipped entirely once ANY ticker was cached, so a warming console showed 3 scanned instead of 51 -> (4) the fallback itself recomputed all 51 chains from a 23 GB table on every request -> (5) ROOT: cache and fallback were treated as alternatives when they are layers - and the expensive layer had no memoisation | Fallback now MERGES per ticker (live cache wins) and is memoised for one loop cycle (RADAR_FALLBACK_TTL_SEC=60). Measured: 5.4 s / 9.8 s per call -> 0.13 s / 0.16 s. Verified in-browser: 11 tracked, 51 scanned, 6 blind. |
| RC-14 | CLOSED | 2026-07-19 | 2026-07-19 | The terrain card and the console header displayed DIFFERENT spot prices for the same ticker at the same moment (743.29 vs 742.49) | (1) the two read different sources -> (2) terrain read `underlying.last` from the option-chain payload while the header read the live quote -> (3) terrain was given its own source so the loop needed only ONE Schwab call -> (4) that choice was made without asking what this repo already treats as authoritative -> (5) ROOT: NO NAMED SINGLE AUTHORITY FOR SPOT existed. Four sources were live - `snapshots.spot` 742.4861, `price_bars_1m.close` 743.18, chain `underlying.last` 743.29, live quote 742.49. Three predate this session; I added the fourth instead of discovering the first. | FIXED SAME DAY, end-to-end, NOT patched. Added `server.resolve_spot()` - the one authority - with explicit precedence (live quote -> chain underlying -> stored snapshot), each result tagged with its source and as-of timestamp. All three terrain consumers (loop, /api/terrain, radar fallback) now call it; direct `chain_underlying_spot` calls outside the authority: 0. Payloads carry `spot_source` + `spot_as_of_ts_utc` so a divergence cannot hide. NEW ENFORCED GATE CHECK `single_spot_authority` forbids reintroducing a second faucet. The client-side override that would have "fixed" the display in one line was deliberately rejected as a patch. NOTE: this row was originally closed on CODE SHAPE and the fix did not actually work - see RC-15, which found the authority returning None on every call. Genuine closure is OBSERVED: resolve_spot("SPY") -> 742.4861 source=schwab_quote_last, matching the console header 742.49. |
| RC-15 | CLOSED | 2026-07-19 | 2026-07-19 | The RC-14 spot fix DID NOT WORK - the card still showed 743.29 against a header of 742.49 after being reported fixed | (1) `resolve_spot` returned source=stored_snapshot, never the live quote -> (2) `_spot_from_quote` returned None on every call despite HTTP 200 -> (3) it read `parsed.get("spot_f")` but the parser returns the key `"spot"` ("spot_f" is a LOCAL VARIABLE NAME inside that function) -> (4) a wrong key is not a crash, not a type error and not a test failure - it silently degraded the authority to its stale fallback -> (5) ROOT: THE FIX WAS NEVER EXERCISED END-TO-END. It was reported working on the strength of "the code now calls resolve_spot" rather than on an observed value. The first regression test written for it ALSO passed with the bug reverted, because it asserted the parser contract instead of driving the function. | Key corrected to `"spot"`; the fallthrough now emits a WARNING naming the ticker and the last/mark it saw, so a dead quote leg can never be silent again. PROVEN BOTH WAYS: with the bug reverted, `test_spot_from_quote_actually_returns_the_quote_price` and `test_resolve_spot_prefers_the_quote_over_the_stored_snapshot` FAIL (2 failed / 6 passed); restored, 8 pass. Live check: `resolve_spot("SPY") -> 742.4861 source=schwab_quote_last`, matching the console header. This also explains the WMT radar-vs-card disconnect: the radar was serving stale-snapshot spot while the card fetched fresh. |
| RC-16 | CLOSED | 2026-07-19 | 2026-07-19 | 743.29 vs 742.49 was not only a key-name bug: the fallback source itself served the PREVIOUS SESSION CLOSE as if it were spot | Operator diagnosed it from market mechanics. (1) 743.29 is Fridays regular close, 742.49 is the post-market last trade -> (2) `chain.underlying.last` returns 743.29 after hours -> (3) so a field NAMED "last" is actually a close once the session ends -> (4) the spot precedence ranked that chain value ABOVE the stored snapshot -> (5) ROOT: SOURCES WERE RANKED BY CONVENIENCE (one fetch) INSTEAD OF BY WHAT THEY MEAN. A stale REAL TRADE is closer to spot than a fresh SESSION CLOSE, and nothing in the code distinguished the two. | OBSERVED on the wire 2026-07-19 after hours: quote.lastPrice=742.4861 (true last trade, postMarketChange -0.8039) while quote.closePrice, quote.mark, regular.regularMarketLastPrice, chains.underlying.last AND chains.underlyingPrice ALL read 743.29. Precedence rewritten to real trade -> stale trade -> session close, with `SPOT_CLOSE_SOURCES` + `spot_is_a_close()` so a close can never be rendered unlabelled. PROVEN BOTH WAYS: reverting to the old chain-before-stored order fails 2 of 10 tests; restored, 10 pass. Live: resolve_spot("SPY") -> 742.4861 source=schwab_quote_last is_a_close=False. |
| RC-18 | CLOSED | 2026-07-19 | 2026-07-19 | The "repo-wide spot migration" was reported as 4 outstanding consumers; the real count and the real defect were both different | (1) I listed market_context.py, market_state.py, planes/context_light.py and features/inference_snapshot.py as second faucets -> (2) reading each showed only ONE is a spot ladder: market_state uses `mark` for OPTION mid-prices (OP-006), context_light WRITES a key named lastPrice from an already-resolved spot, and inference_snapshot holds a field-name mapping dict -> (3) so three of the four were never faucets at all -> (4) I had counted grep hits on field NAMES instead of reading what each site does -> (5) ROOT: SCOPE WAS ESTIMATED FROM A TEXT SEARCH RATHER THAN FROM READING THE CALL SITES, which inflated the work item and delayed the one fix that mattered | MEASURED by reading all four sites. The single real faucet, `market_context._extract_quote`, carried the RC-16 defect: `regularMarketLastPrice` and `quote.mark` sat inside a "last" ladder, and both are the regular CLOSE after hours. Extracted `_last_traded_price()`: real trades only (quote then extended), close as an explicitly-labelled last resort, and no `or` chaining so a legitimate 0.0 cannot fall through. PROVEN: restoring the old ladder fails `test_mark_is_never_used_as_a_last_price` (1 failed / 5 passed); restored, 6 pass. Honest note - only 1 of the 6 new tests distinguishes old from new; the other 5 pass under both and are contract guards, not regression guards. |
| RC-19 | CLOSED | 2026-07-19 | 2026-07-19 | The gate file hit 807 lines against an 800 ceiling and I split it in two, creating bloat to move a counter | (1) file_length went 38 -> 39 and the ratchet blocked the commit -> (2) I split the governance checks into a new module -> (3) those checks reference `Violation` and `CHECKS`, which live in the gate, so the split was circular -> (4) I added FIVE circular-import workarounds (TYPE_CHECKING plus call-time imports) to make the split compile -> (5) ROOT: A NUMERIC THRESHOLD WITH NO JUSTIFICATION PATH FORCES A NUMERIC REMEDY. The only question I asked was "is the number under the limit?" instead of "does splitting this improve the code?" - which it did not. | MEASURED: 5 circular-import workarounds and a 208-line module, created to save 7 lines. REVERTED - merged back to one file (823 lines) and the split module deleted. `check_file_length` now accepts `# institutional-length-ok: <reason>`, the same escape the complexity check already offered, so a threshold prompts a JUDGEMENT rather than a reflex. The gate file declares its own justification: it is one concern, its length is the sum of its checks, and splitting by line count made it worse. VERIFIED: file_length back to 38, debt_ratchet PASS. |
| RC-20 | CLOSED | 2026-07-19 | 2026-07-19 | `build_model_serving_provenance` reported `artifact_sha256: None` for every VERIFIED artifact | (1) a test asserted `sha is None or isinstance(sha, str)`, true for every value -> (2) replacing it with a real assertion made it fail immediately -> (3) the serving surface projects a key named `artifact_sha256` -> (4) the verifier in active_bundle_contract emits `expected_sha256` and `actual_sha256` and has NEVER emitted `artifact_sha256` -> (5) ROOT: THE PROJECTION READ A KEY NAME THAT WAS NEVER PRODUCED, so a provenance record claiming VERIFIED_AGAINST_BUNDLE_MANIFEST exposed nothing that was actually verified - and a vacuous assertion guaranteed no test would ever notice | OBSERVED: `assert isinstance(sha, str) and len(sha) == 64` failed with `got None`. Projection now maps `artifact_sha256` from `actual_sha256` (the digest computed off disk during verification) and additionally exposes expected_sha256/actual_sha256. VERIFIED: 57 passed in test_model_contract_enforcement.py. This is the same failure class as RC-15 (`spot_f` vs `spot`) - a key-name mismatch producing a silent None. |
| RC-21 | CLOSED | 2026-07-19 | 2026-07-19 | The SAME defect class shipped twice in one session: a dict key read that no producer ever writes, yielding a silent None | (1) RC-15 read `parsed.get("spot_f")` when the producer emits `"spot"`; RC-20 read `p.get("artifact_sha256")` when the verifier emits `"actual_sha256"` -> (2) both returned None on every call and degraded silently instead of failing -> (3) mypy cannot see it because these are `dict[str, Any]`, which accepts any key -> (4) ruff cannot see it because a string literal is valid syntax, and tests did not see it because both were guarded by assertions that could not fail -> (5) ROOT: NO TOOL IN THE STACK VALIDATES STRING KEYS AGAINST THEIR PRODUCERS. In Python a misspelled or stale key is not an error, it is a None - so this class is invisible to every gate we had | MEASURED by prototyping BEFORE building: an AST cross-reference of every literal dict READ key against every literal WRITE key found 313 raw candidates, reduced to 124 after excluding route decorators and environment variables. Shipped as `check_no_orphan_dict_keys`, ADVISORY at 232 and ratcheted, NOT enforced - vendor payload keys (Schwab quote/chain nodes, news APIs) are legitimately never written by us, so a zero-tolerance rule would be false. Precedent: enforcing an unvalidated rule is what made `tests_must_assert` flag 14 legitimate tests. Already surfaced a real lead: `chain_json.get("underlyingPrice")` at server.py:11901 computes charm against a value that is Friday CLOSE after hours (RC-16 class) in the debug endpoint. |
| RC-22 | CLOSED | 2026-07-19 | 2026-07-19 | Radar proximity was measured in PERCENT, which means different things on different instruments | (1) a 0.5% gap is a normal hour on SPY and noise on TSLA -> (2) so one percent threshold cannot classify both -> (3) the radar was therefore ranking contacts by a scale-free number -> (4) the operator asked for the ring metric and I had no principled answer -> (5) ROOT: DISTANCE WAS NOT NORMALISED BY WHAT THE INSTRUMENT CAN ACTUALLY TRAVEL, so "close" was undefined across the board | ATR normalises it. PROTOTYPED FIRST against price_bars_1m: daily and 15-minute ATR computed for SPY/QQQ/IWM/NVDA/TSLA/WMT, ratios 4.8x-9.2x, consistent with ~26 fifteen-minute buckets per session. `terrain_atr.py` ships rings derived from meaning rather than invented: CONTACT 0.10, CLOSING 0.35, SECTOR 0.75 daily ATR, REGIME 0.15. Missing ATR yields no ring rather than a guess (verified: ring_for(5.0, None) is None). OBSERVED live: 51 scanned, 25 tracked, SPY 1pt = CLOSING while QQQ 1pt = CONTACT off the same gap. 7 tests in tests/test_terrain_atr_radar_v1.py. |
| RC-23 | CLOSED | 2026-07-19 | 2026-07-19 | The whole terrain UI script silently died: every ed* function undefined, tab inert | (1) `edSwitchView is not defined` in the browser -> (2) the console showed SyntaxError: Invalid or unexpected token -> (3) a backslash-n I wrote inside a JS string literal reached the file as a REAL line break, splitting the string across two lines -> (4) one broken literal aborts parsing of the entire script block, so every function in it vanished -> (5) ROOT: ESCAPED NEWLINES PASSED THROUGH THE HEREDOC/PYTHON EDIT PATH ARE MANGLED, and a broken string does not fail loudly - it silently removes every function defined after it | OBSERVED: read_console_messages showed two SyntaxErrors; typeof edSwitchView was "undefined" while the DOM elements existed. Rebuilt the tooltip using String.fromCharCode(10) so no escape sequence passes through the edit path at all. VERIFIED after reload: edSwitchView is a function, radar renders 14 contacts, tooltip reads "NET - REGIME CHANGE gamma flip 278.91 // 0.13 daily ATR | 0.9x 15m ATR". This mangling has recurred all session (it also corrupted two write_text calls in the gate); fromCharCode is the reliable form. |
| RC-24 | CLOSED | 2026-07-19 | 2026-07-19 | Unchecking VOICE did not stop the voice; and ALERTS had no sound at all | (1) the terrain VOICE checkbox set edAlertsVoice, which gated only terrain-alert speech -> (2) the voice CLOCK is a second consumer that calls window.edSpeak directly -> (3) so muting terrain left the clock talking and the control looked broken -> (4) separately, alerts only ever flashed and spoke, so there was no tone to turn on or off -> (5) ROOT: A TOGGLE GATED ONE CONSUMER INSTEAD OF THE SHARED OUTPUT. Mute belongs at the single speech function every consumer routes through, not at one caller. | OBSERVED before the fix, in-browser: with terrain VOICE unchecked, terrain alerts spoke 0 times but window.edSpeak still spoke - and a regex scan for AudioContext/new Audio/.play() across the document returned FALSE, confirming no alert sound existed. FIXED: window.edSpeak now checks window.edVoiceMuted first, so muting silences every consumer including the clock; added edTone() with four distinguishable WebAudio patterns (rising=approaching, falling=break, two-tone=regime, low=confidence lost); three independent toggles VOICE/SOUND/ALERTS persisted to localStorage and restored on open. VERIFIED in-browser: VOICE off suppresses clock speech (true), VOICE on restores it (true), SOUND and ALERTS persist 0/1 correctly, all three restore. |
| RC-25 | CLOSED | 2026-07-19 | 2026-07-19 | The terrain tab used a fraction of the screen and required scrolling | (1) it rendered a single 900px card centred in the viewport -> (2) the radar sat beneath it, off-screen -> (3) the layout had no height contract, so content defined the page height -> (4) any added panel pushed the page taller and forced scrolling -> (5) ROOT: THE VIEW WAS SIZED BY ITS CONTENT INSTEAD OF BY THE VIEWPORT, so real estate was left unused and new information always cost a scroll | Rebuilt as a three-column deck (radar rail / hero / periphery) whose height is set from the viewport at render and on resize. MEASURED in-browser at 1280x720: deck height 547px, bottom edge 658 of 720, document scrollHeight within the viewport so PAGE SCROLLS = false; columns 170 | 898 | 210. Radar is grouped BY RING with glyphs as a second channel alongside colour. Also fixed a cross-payload dependency found during verification: the ATR stat row read ATR out of the RADAR response, so a slow radar poll blanked every ATR figure while terrain itself was fine - /api/terrain now carries atr_daily and atr_15m itself (verified 5.987 / 1.233 for SPY). |
| RC-26 | CLOSED | 2026-07-19 | 2026-07-19 | The radar scope stayed empty for ~20 s after opening the tab, reading as broken | (1) the first sweep took 40.5 s measured -> (2) the cold path called resolve_spot per ticker, i.e. 51 live Schwab quote round-trips, for a scanner that only needs a scale -> (3) removing those left 22.5 s, of which ~11.5 s was ATR because _MAX_1M_BARS read 40,000 one-minute rows per ticker -> (4) the remainder is reading 51 chain payloads out of the 23 GB snapshots table (RC-6) -> (5) ROOT: A SWEEP OVER MANY TICKERS WAS BUILT FROM PER-TICKER PRIMITIVES SIZED FOR ONE TICKER, so every per-ticker cost multiplied by 51 and landed on the operator first click | MEASURED at each step: 40.5 s -> 22.5 s (dropped the 51 live quotes; stored spot is itself persisted from quote.lastPrice) -> ATR window cut 40,000 to 24,000 rows, 26.1 s to 11.5 s across 51 with values UNCHANGED (SPY daily 5.987 / 15m 1.233 before and after). A first attempt at 12,000 rows was VERIFIED WRONG - it returned daily ATR None because price_bars_1m carries extended-hours bars, about 1,000 per session not 390, so 12,000 gives only 12 sessions against the 15 that ATR(14) needs; measured floor is 20,000. Remaining cold cost now runs in a boot prewarm thread off the request path, so the observed request is 0.13 s, and the rail shows a Scanning airspace state while warming. |
| RC-27 | CLOSED | 2026-07-19 | 2026-07-19 | Terrain text used muted greys the operator cannot comfortably read - session labels, radar level lines, ATR figures, stat sub-values | (1) two palette variables carried everything secondary: --tv-mu #8ba0b8 and --tv-dim #5d7089 -> (2) both are low-luminance greys on a #0d131a panel -> (3) they were applied to session keys, the radar level line, stat labels and sub-values, the ladder percent column and two of the four radar ring groups -> (4) VISUAL HIERARCHY WAS BEING EXPRESSED BY DIMMING rather than by size and weight -> (5) ROOT: THE DESIGN USED CONTRAST AS A RANKING DEVICE, which spends the one channel a low-vision reader depends on; rank is free in size and weight, and contrast is not ours to spend | MEASURED before and after with WCAG relative-luminance contrast computed in-browser against each element true background. Both variables raised (--tv-mu to #dceaf7, --tv-dim to #cfe3f5) and the two ring groups that used them given real colours (CLOSING green #3fdd88, IN SECTOR blue #4fc6ff). Hierarchy now comes from size and weight only. RESULT: all 12 sampled elements at AAA, range 7.3:1 to 17.3:1 - session key 14.2, session value 17.3, radar level 15.2, radar ticker 17.3, radar ATR 7.3, stat label 13.5, stat sub 16.4, ladder percent 14.5. MAX PAIN was greyed to signal advisory status; it is now purple and labelled MAX PAIN (advisory), so the meaning is carried by words rather than by dimming. Layout unchanged: deck bottom 716 of 720, no page scroll. |
| RC-28 | CLOSED | 2026-07-20 | 2026-07-20 | The terrain card lagged the console header - operator saw 745.10 on the card against 744.98 in the header, eventually converging | (1) both now resolve spot through the same authority, so the SOURCE was not the problem -> (2) `/api/terrain` returns the cached payload verbatim when the terrain loop has one -> (3) that payload was built by the loop up to 60 s earlier, with the spot of that instant baked into it -> (4) the UI then polled that same frozen payload every 15 s, so the worst case was ~75 s behind a sub-second SSE header -> (5) ROOT: A FAST-MOVING QUANTITY WAS FROZEN INSIDE A SLOW-MOVING CACHE. Levels are correctly cached for 60 s; spot never can be, and RC-14 recorded exactly this as the durable fix without implementing it | REPRODUCED first: cache entry 745.10 against a live quote of 744.88, delta 0.22, matching the operator screenshot. FIXED by separating the two rates: TerrainSnapshot now carries the 241-point gamma profile (excluded from the wire payload, which stays ~1.1 KB), the loop caches it beside the payload, and a cache hit re-resolves spot and recomputes the REGIME as the sign of that cached profile at the fresh spot - so the regime can never disagree with the price printed beside it. Levels, walls and flip stay cached. MEASURED after: endpoint serves 745.1259 against a live 745.1259, delta 0.0000. UI poll cut 15 s to 5 s since a cache hit is one quote call. PROVEN BOTH WAYS at the ENDPOINT: reverting to `return cached` fails test_terrain_ENDPOINT_serves_live_spot_from_a_cached_payload (1 failed / 12 passed); restored, 13 pass. The first test written for this called the helper directly and passed WITH the bug reverted - a test that cannot fail where the bug lives - so it was rewritten to drive get_terrain() itself. |
| RC-29 | CLOSED | 2026-07-20 | 2026-07-20 | The card STILL trailed the header after RC-28 - two faucets to the same number | (1) RC-28 removed the 60 s cache freeze but the card still lagged -> (2) the header does not read spot from the server at all: it renders `window._fastLaneSpot`, a value the browser already holds and repaints sub-second off the quote stream -> (3) the card was doing its own /api/terrain round-trip for a SECOND COPY of that number -> (4) so the card could never be fresher than its poll interval no matter how fast the endpoint got -> (5) ROOT: I BUILT A SECOND PATH TO A NUMBER THE BROWSER ALREADY HAD. One faucet means the same VALUE in the same process, not merely the same upstream field | The card now renders `window._fastLaneSpot` itself, and the ladder exposes its price scale so the spot line and the ATR distances repaint from that live value between level polls. An independent 400 ms repaint timer was MEASURED FIRST and still showed 2 mismatches in 24 samples, always exactly one quote apart, because the header repaints on the stream tick and the card repainted on its own clock - so the paint is now hooked to `refreshUtilityBar` and both numbers are written in the same call. VERIFIED in-browser: 40 samples over ~12 s across three distinct price ticks (745.17, 745.21, 745.22), MISMATCHES 0. Levels still arrive on the slow poll; only spot-dependent rendering is live. |
| RC-30 | CLOSED | 2026-07-23 | 2026-07-23 | Capture recorder sat with BOTH websockets (Schwab L1 + Alpaca IEX) silent for 4+ minutes mid-RTH while the process looked healthy; separately, four task-launched recorder instances died with console-interrupt exit 0xC000013A | (1) feeds went quiet simultaneously -> (2) `py-spy dump --pid 35488` showed the event loop idle at _poll, no error anywhere -> (3) a network blip left the TCP sockets HALF-OPEN: recv() never returns and never raises -> (4) both reconnect loops were ERROR-DRIVEN only (alpaca looped on 1s recv timeouts without a staleness check; the Schwab pump awaited handle_message indefinitely) -> (5) ROOT: reconnect logic keyed on exceptions cannot see the one failure mode that raises nothing. Task deaths: separate root — `Export-ScheduledTask` showed DisallowStartIfOnBatteries=true AND StopIfGoingOnBatteries=true on a DELL laptop (Win32_Battery present); Task Scheduler killed the running task on battery transitions (explains the seconds-fast death #4; the scheduler-free hidden direct-run survived all day) | FIX 1 (code): staleness IS the trigger now — pure decision fn `stream_needs_recycle` (stale>90s AND data-seen AND cooldown>180s), Schwab stream rebuilt clean via _schwab_connect, alpaca session returns-to-reconnect after 120s of silence. Seam tests: test_stream_needs_recycle_decision_boundaries + test_alpaca_session_recycles_on_silent_socket (fake half-open socket; 13 passed, `python -m pytest tests/test_stream_capture_daemon_v1.py -q`). FIX 2 (config): Set-ScheduledTask AllowStartIfOnBatteries + DontStopIfGoingOnBatteries applied and re-exported (DisallowStartIfOnBatteries=false, StopIfGoingOnBatteries=false). Mid-session recovery same day: recorder bounced 12:5x, flow verified (newest row 1s, session 1 totals 5,840,795 quotes / 37,456 IEX prints). |
| RC-17 | CLOSED | 2026-07-19 | 2026-07-23 | `test_mkt_ctx_*` in tests/test_analytics_state_freshness_api.py fail intermittently, turning the close-out red at random | (1) they pass in isolation (2 passed) but fail inside the file -> (2) two consecutive runs of the SAME file gave 99 passed then 1 failed with no code change -> (3) they synchronise on `threading.Event.wait(5)` plus the module-global `_mkt_ctx_refresh_inflight` flag -> (4) the assertion therefore depends on a background refresh thread being SCHEDULED inside a fixed wall-clock window -> (5) ROOT: THE TEST ASSERTS ON WALL-CLOCK TIMING OF A BACKGROUND THREAD instead of on a deterministic synchronisation point, so under CPU load it measures machine contention rather than correctness | OPEN WITH A STATED REASON, not a punt. MEASURED: same file, back-to-back, 99 passed then 1 failed; the two named tests pass alone. The fix is not in the test - it needs the production path to signal a real handshake (an Event set when the refresh enters its critical section) so the test can wait on an EVENT rather than a TIMEOUT. That changes threading behaviour in the live server and must be done as its own supervised change with the operator present, not appended to the end of a long session. Until then a red close-out on these three tests specifically is a known flake, and re-running until green would be exactly the dishonesty this log exists to prevent. RE-DATED 2026-07-23 → 2026-07-30: due slipped during chart nav push; handshake still needs a supervised session — not closed, not forgotten. CLOSED 2026-07-23, operator present and authorizing ("why can't you fix that now"). The handshake did NOT need a production change: the fake sweep injected via monkeypatch IS the deterministic sync point — tests now wait on the fake's own `done` Event and assert MECHANICS, not clocks: not-inline is proven by (a) callers receiving the OLD context and (b) sweep-thread identity != caller thread; concurrency tests start callers through a `threading.Barrier` so overlap is certain instead of scheduling luck; every remaining wait is a 30s UPPER bound with a liveness assert (`join(30)` + `not t.is_alive()`), which fails only when genuinely broken. The `inline_elapsed < 0.15` wall-clock assert (the actual flake) is deleted. Production threading untouched — no money-path churn to serve a test. EVIDENCE: `python -m pytest tests/test_analytics_state_freshness_api.py -q` → 99 passed; `-k mkt_ctx` run 10 consecutive times → 6 passed every run (the original failure signature was 99-then-1-failed across two consecutive runs). |
| RC-6 | OPEN | 2026-07-19 | 2026-07-26 | `data/ed_console.db` is 24.60 GB for 286,142 snapshot rows; a single cold row read costs ~700 ms | THE ORIGINAL WHY-CHAIN WAS WRONG AND IS REPLACED; SO WERE THE FIRST REPLACEMENT'S NUMBERS. The original asserted the root was chain JSON stored inline in `snapshots`. Corrected chain, EXACT `SUM(LENGTH(...))` AS-OF 2026-07-20T11:28:47Z (the file is written continuously, so every byte figure here is only true at its stamp): (1) `option_chain_json` is 5.10 GB in `snapshots` (286,325 rows) and 4.88 GB in `snapshots_1m_normalized` (178,786 rows); `replay_context_json` is 1.38 GB and 1.34 GB -> (2) the normalized side, 6.22 GB, is a SECOND COPY of payloads already in `snapshots` - 27% of the 22.94 GB file -> (3) the copies are byte-identical: 200/200 sampled (ticker, ts_utc) pairs matched exactly, and of 149,011 normalized rows carrying a chain, 149,011 have the same chain in `snapshots` and ZERO are orphans (independently reproduced by Cursor 2026-07-20) -> (4) the copy is not a decision anyone made: `snapshot_normalizer._normalized_insert_columns` builds the INSERT as "every `snapshots` column that also exists on normalized", so the blobs were carried automatically when the tables were aligned for Issue 16 -> (5) ROOT: A GENERATED COLUMN LIST HAS NO WAY TO SAY "THIS COLUMN IS A REFERENCE, NOT A VALUE". The normalized row is addressed by (ticker, ts_utc) - exactly the `snapshots` key - so the payload was always reachable by join and never needed storing again. ONE FAUCET was violated at the storage layer, and nothing asked the question because the INSERT is generated rather than written. | OPEN WITH MEASURED SCOPE, not started. Consumers enumerated by reading, not by counting hits: 89 live code files (.py/.sql) reference the table - 115 including 22 docs and 4 archive copies - but only 7 reference the blob column, and of those only `live_vs_replay_validation.py` actually PARSES a chain from normalized (line 81, with `snapshots_1m_normalized` in `_VALIDATION_TABLES` line 40); `replay_bundle_coverage.py` only COUNTS presence via `length(...) > 10`; `backfill_flow_imbalance.py` reads the chain from `snapshots` and merely re-materializes after. Fix direction (end-to-end, not a patch): stop the normalizer copying reference columns, resolve the payload for that one consumer through the (ticker, ts_utc) join, then reclaim. NOT EXECUTED - and the migration plan is NOT YET SAFE TO WRITE, because of the open question below. METHOD FAILURE, recorded because it is the reusable lesson: the first replacement chain quoted 5.96/5.78/1.03/0.92 GB and a "~7.7 GB second copy". Those came from SAMPLING 1,500 rows per table and extrapolating, when an exact `SUM(LENGTH(...))` was available and nothing forced the shortcut. Cursor's audit refuted every one of them; the exact figures are above. A sampled number presented in the same voice as a measured one is the defect, independent of how close it lands. OPEN QUESTION blocking the migration: the file was 24.60 GB earlier on 2026-07-20 and is 22.94 GB at the stamp above, yet `auto_vacuum=NONE` and `freelist_count=0`, and no chain-pruning or VACUUM statement exists anywhere in the repo (searched .py/.ps1/.bat/.sh/.sql/.md/.json/.yml - 3 files mention VACUUM, none execute one against this DB). SQLite does not shrink under those conditions without a VACUUM. LEAD, not yet confirmation: `normalized_training_sync.py:369` refers to "the host VACUUM" as an existing thing the WAL truncation is sized around - i.e. the reclaim mechanism lives OUTSIDE version control, which is why the repo search is empty. That would explain the 1.66 GB, but nothing observed proves one ran at ~06:30 today. OPERATOR INPUT REQUIRED: is there a scheduled/manual host VACUUM on `data/ed_console.db`? Until answered, any claim about what a column drop would reclaim - or whether a manual VACUUM is even needed - is unfounded, and the supervised migration must not be scheduled on it. An unversioned maintenance step acting on the production DB is itself worth capturing. |
| RC-32 | CLOSED | 2026-07-23 | 2026-08-06 | Production daily scoreboard headline metrics (accuracy/balanced-accuracy/MCC/confusion, v4 trade-call quality) INCLUDE rows whose attached outcome is anchored ≥1 bar in the decision's FUTURE — MEASURED 2026-07-23: 28,622 of 65,247 nearest_within_tol rows (43.9%; ~15% of all decision rows) have matched_snapshot LATER and cross-minute, with join deltas persisting to ±58.9s (legacy wider-tol attaches re-tagged 'nearest_within_tol' by the backfill resync at calibration/backfill_outcomes.py L245, so the method tag cannot distinguish 5s from 59s) | (1) headline metrics include look-ahead rows -> (2) `_v4_accumulate` (daily_scoreboard.py L824-833) scores every truth-valid row into the confusion and only COUNTS the join cohort beside it; `_v4_cell_warnings` L947-949 appends TIMESTAMP_IDENTITY_NOT_PROVEN when nearest/unknown cohorts > 0 -> (3) the v4 cohort taxonomy was built as disclosure context, not as a scoring gate -> (4) the design comment (L165-166 "never silently pooled") was satisfied by ANNOUNCING the pooling instead of preventing it -> (5) ROOT: DISCLOSURE ACCEPTED AS A SUBSTITUTE FOR EXCLUSION AT A SCORING BOUNDARY — a warning attached to a metric does not remove contaminated rows from the metric | FIXED 2026-07-23 (Claude direct, after four Cursor ghost-edit failures — IDE never wrote the file): `_V4_SCORED_JOIN_COHORTS = (identity, exact_timestamp, nearest_earlier)` explicit-membership allowlist; `_v4_accumulate` counts every cohort for disclosure but returns before the confusion matrix for nearest_later/unknown_join, incrementing per-cell `n_excluded_lookahead_join` (surfaced by `_finalize_v4_cell`); `bundle_identity_unproven` now counts scored rows only. PROVEN: `tests/test_daily_scoreboard_rc32_cohort_gate.py` 4 passed (incl. flood test — 50 "perfect" look-ahead rows cannot move accuracy) + full `-k daily_scoreboard` suite 86 passed. OBSERVED LIVE both ways: recomputed 2026-07-22 (clean era) → 11,926 scored, all exact_timestamp, 0 excluded; recomputed 2026-06-09 (tolerance era) → 2,376 scored (1,816 exact + 560 nearest_earlier), **304 nearest_later rows EXCLUDED and disclosed**. Durable retirement of the class stays the §9.1 bar_start stamping fix. Scope note: scoreboard/quality-circle surface only — money path is WAIT and the FP battery never read these joins. |
| RC-31 | OPEN | 2026-07-23 | 2026-08-06 | Bar-based study features bleed extended-hours and OVERNIGHT data: `research/tcn_eval_v1/runner.py::_load_closes` (L68-77) selects ALL of `price_bars_1m` with NO session filter, and `_build_xy`/`har_features` take `np.diff` over the full close series — so the overnight gap enters as a fake "1-minute" squared return and pre/post-market bars enter TCN lookback windows and HAR realized-vol means. Shared: HAR-RV runner imports `_load_closes`/`_load_labeled_rows` from the TCN runner (har_rv/runner.py L21); `_load_labeled_rows` likewise has no session filter, and HAR SPY n_scored 30,303 over 74 days ≈ 410 rows/day (> 390 RTH bars/day) is consistent with extended-hours decision rows being scored. Kalman CONFIRMED same-affected 2026-07-23: `research/kalman_eval_v1/runner.py` L19 imports `_load_closes`/`_load_labeled_rows` from the TCN runner, and its Kalman filter runs over the full close series (overnight jumps enter as innovation spikes) — affected set is TCN + HAR-RV + Kalman. MEASURED supporting evidence (read-only sweep 2026-07-23, `reports/f4_synthetic_label_sweep_v1.json`): SPY has 45,233 outcome_60c-labeled snapshot rows (~611/day >> 390 RTH minutes — extended-hours labeling confirmed at scale). SELF-CORRECTION on the synthetic rate: the first probe counted `source != schwab_1m_accumulator_sqlite` as synthetic (50.53%), which WRONGLY included `schwab_pricehistory` (133,061 REAL Schwab history-API bars). Corrected synthetic set = {synthetic_interior_grid_repair_v1 (169,929), synthetic_edge_carry_v1 (27), gap_fill_canonical_1m_grid_v1 (10)}. CORRECTED RATES: whole store, RTH-only labeled rows with a synthetic FORWARD bar = 17.5% (1c) / 15.1% (5c) / 17.4% (15c) / 21.1% (60c); sentinels 60c RTH: SPY 10.96% (2,819/25,716), QQQ 17.07%, IWM 16.78%; all-sessions rates run 20.8–25.5%. Roughly 1 in 6 RTH labels on the study grid was graded against an interpolated close that no market printed. ANCHOR-SIDE MEASURED 2026-07-23 (index-aligned join, 1.2s): of 138,085 RTH 60c-labeled rows, 12.84% (17,726) have a SYNTHETIC anchor bar and a further 4.46% (6,163) have NO bar at the floor minute (true anchor is an older close — staleness magnitude unmeasured). HOLIDAY DIMENSION (F-9 census, same day): market_session='rth' is stamped by CLOCK with no trading calendar — Memorial Day 2026-05-25 (market CLOSED) has 3,795 labeled 'rth' rows and 2026-07-03 (closed, July-4 observed) has 912; 4,707 frozen-quote holiday rows sit in the training-eligible universe teaching "market flatlines". Same root: session universe (now including CALENDAR) unstated at every consumer. IN-MATRIX INSTANCE (Round 2 Session 1, 2026-07-23): `ml_train.engineer_features` L468-473 — `dgex = net_gamma.groupby("ticker").diff()` with NO day term, so at every session open dgex = today's first net_gamma minus YESTERDAY'S LAST (overnight positioning jump presented as a 1-minute change); same class for the `net_gamma_prev` attach. CONTRAST (verified clean): the LSTM/Transformer sequence builders group by day and slide windows strictly WITHIN each day (`lstm_data.build_lstm_dataset` L1028-1039, `transformer_train.prepare_transformer_data` L205-210) — sequence windows never span the overnight gap; their residual is intra-day coverage gaps being unencoded (adjacent steps can be minutes apart), plus the shared era/value corruption. | (1) features include EH bars and cross-session returns -> (2) the loader queries the whole bars table with no time predicate -> (3) `price_bars_1m` carries extended hours BY DESIGN (~1,000 bars/session, measured in RC-26) and the loader silently assumed bars==RTH -> (4) no contract names which session universe study features/labels must live in (the ml_train path claims RTH filters in `load_data`; the bar-path runners have no equivalent and nothing forced the question) -> (5) ROOT (provisional, chain not exhausted): SESSION UNIVERSE IS AN UNSTATED ASSUMPTION AT EVERY BAR CONSUMER instead of a named, tested parameter with one authority (time_et RTH constants exist and were not consulted here). SCOPE WIDENED 2026-07-23 (Round 1A label audit): the LABEL WRITER is a second instance of the same root — `db.py::fill_outcomes` (L3315-3425) and `_apply_bar_based_outcome_updates` (L4764-4917) carry NO `market_session` predicate (snapshots carry the tag; the WHERE clause never reads it), and `forward_bar_start_utc` is pure wall-clock arithmetic with NO session bridge — so (a) premarket/afterhours snapshots receive labels computed from extended-hours bars, and (b) late-RTH snapshots are labeled against POST-MARKET forward bars when those bars exist (60c: every snapshot after 15:00 ET ≈ 15.4% of RTH rows; 15c after 15:45 ≈ 3.8%; 5c ≈ 1.3%; 1c ≈ 0.3%), else NULL (matches FP-02 "missing forward 1m bars" debt). Mitigating fact: labels are retroactively cohort-able by joining snapshots.market_session — the contaminated set is identifiable without recompute. | NOT FIXED — recorded during the feature-pipeline adversarial audit (operator-approved 2026-07-23). Fix belongs to the harness rebuild, not to the voided v1 runners: session-universe becomes an explicit loader parameter sourced from time_et RTH authority, with a seam test proving overnight returns cannot enter a feature window. Affects interpretation of the HAR-RV QQQ open lead: its RV features included overnight-gap spikes, so the lead must be re-measured under clean session handling before promotion. |
| RC-7 | CLOSED | 2026-07-19 | 2026-07-26 | `research/cost_aware_eval_v1/faint_lead_kill_v1.py` and `stress_survivor_v1.py` had no test referencing them | (1) they were added as research scripts, not production modules -> (2) the close-out orphan check treats every non-tests/ non-tools/ file as production -> (3) so they were flagged on every single run -> (4) a warning that fires every run and is never actionable trains the reader to skip the whole block -> (5) ROOT: RESEARCH CODE WAS TREATED AS UNTESTABLE BY CONVENTION while its OUTPUT was being cited in decisions - a study whose import is broken or whose statistical control degenerates yields a confident wrong number rather than an error | FIXED, not exempted: `tests/test_cost_aware_eval_research_smoke_v1.py` added - import integrity, a shared pinned SEED across both studies, the sign-shuffle control returning a populated result distinct from the observed statistic, `_eval_xy` signature contract, and determinism of `evaluate` on fixed input. OBSERVED: 5 passed. The first draft of these tests was written against GUESSED signatures and 2 of 4 failed; rewritten against the real ones via inspect.signature. |
| RC-33 | CLOSED | 2026-07-24 | 2026-07-31 | The terrain verdict shown to the operator could not be reconciled with the analytics-state terrain fields: /api/terrain reported SHORT_GAMMA_TREND / TRUSTED while /api/analytics/state reported terrain_regime UNAVAILABLE / STAND_ASIDE for SPY at the same instant | (1) two endpoints compute terrain -> (2) the UI reads ONLY /api/terrain; the analytics-state terrain_* fields are read by nothing (whole-repo consumer audit: written at server.py:8522-8525, zero reads anywhere — no module, no downstream code, no test, no tool) -> (3) they disagree because each runs build_terrain_read on a DIFFERENT chain: /api/terrain on the wide-capture multi-expiry chain (515-960, 199-strike, ±30%, TRUSTED) vs analytics-state on the selected-0DTE slice alone (729-748, 20-strike, ±1.3%, untrusted) -> (4) a second terrain read was attached to the analytics pipeline on that pipeline's near-spot contracts and left behind after terrain moved to its own /api/terrain product -> (5) ROOT: TERRAIN HAD TWO SOURCES OF TRUTH — a dead, thinner duplicate emitting a contradictory verdict. Same class as RC-14 (no single spot authority) and the RC-11 note (two surfaces stating opposite regimes for one ticker). | Stripped the dead terrain_* attachment from server.py so /api/terrain is the single terrain source of truth, computed on the wide-capture multi-expiry chain (operator decision 2026-07-24: full-chain basis, because dealers hedge the whole delta book across weekly/monthly expiries, not the 0DTE slice). The flip-confidence gate (±5% span floor) is RETAINED as the fail-closed backstop for when wide capture is unavailable — the fix is single-source, never gate-weakening. VERIFIED: chain spans re-run via `curl -s 'http://127.0.0.1:8000/api/terrain?ticker=SPY'` (flip_diag strike_lo/strike_hi/n_strikes = 515/960/199, TRUSTED) vs `curl -s 'http://127.0.0.1:8000/api/analytics/state?ticker=SPY'` (kl_gamma_flip_diag = 729/748/20, untrusted); gate-retention test `python -m pytest tests/test_terrain_engine_v1.py -q` (18 green) asserts the ±1.3% real fixture still fails closed to STAND_ASIDE; dead-field consumer audit found zero reads repo-wide; live split logged in reports/terrain_divergence_monitor_v1.jsonl (tick 0 — card SHORT_GAMMA_TREND chain 515-960 vs dead-field UNAVAILABLE chain 729-748). END-TO-END: cut the divergent producer (the server.py analytics-state terrain block), not the symptom, and locked the single source so a second terrain read cannot reappear; full-chain basis recorded in DERIVED_ANALYTICS_REGISTRY.md and terrain_engine.compute_terrain. |
| RC-34 | CLOSED | 2026-07-24 | 2026-07-31 | The ticker control (symbol input + dropdown) was not visible on the console; the operator could not see or change the active ticker | (1) the .ub-ticker-block renders at zero width so its input has no room -> (2) it is the ONLY utility-bar block allowed to shrink (flex:0 1 auto, min-width:0, overflow:hidden) while every sibling keeps its width, so all the bar's overflow pressure lands on it -> (3) the bar's blocks (brand, ticker, bid/ask, vix, expiry, refresh, pills, viewtabs, theme, clock) sum wider than the bar, so the one shrinkable block is squeezed to nothing -> (4) the bar gained blocks past its width as features grew and nothing reserved the ticker's space -> (5) ROOT: A PRIMARY CONTROL WAS ENCODED AS THE LAYOUT'S MOST EXPENDABLE ELEMENT — criticality was never expressed in the CSS, so the always-needed ticker was the sole shrink victim instead of the one block guaranteed its width, and inevitable crowding hid exactly the control that must never vanish. Same UI-defect family as RC-8 (levels overlapped/unreadable). | Fixed .utility-bar .ub-ticker-block: flex-shrink 1 to 0 (flex:0 0 auto), min-width 0 to max-content, overflow hidden to visible, so the ticker cannot collapse; crowding now spills into the bar's existing horizontal scroll of lower-value right-side items. Pure CSS, no logic change, served fresh from disk (no restart). VERIFIED on a clean reload: `python -c "import urllib.request as u; print('min-width: max-content' in u.urlopen('http://127.0.0.1:8000/').read().decode())"` returns True — the served .ub-ticker-block rule now carries the fix; a browser bounding-box read on a fresh load (no DOM hack) shows the block restored from zero to full content width with #ticker-input value SPY visible. END-TO-END: protected the primary control at the CSS rule rather than nudging one viewport, and removed the shrink victim so no future bar crowding can re-hide it. |
| RC-35 | CLOSED | 2026-07-26 | 2026-08-02 | compute_net_charm computed charm_unit = -phi(d1)*d2/(2T), the exact NEGATIVE of calendar-time charm, so charm_direction was inverted (fed a displayed note + the fusion model feature); distinct from RC-2 (bs_charm sign) and RC-3 (call/put convention), which both left this untouched | (1) charm_unit negated calendar charm -> (2) a prior revision negated the formula on the written claim "ours = -dDelta/dT" -> (3) the quoted textbook formula was ALREADY calendar-time charm, so dDelta/dT versus dDelta/dt was conflated -> (4) RC-2 set the standard "every numerical function ships an independent re-derivation test" but it was applied to bs_charm and never to compute_net_charm -> (5) ROOT: a second charm implementation duplicated the math and escaped the RC-2 finite-difference standard, so its sign was never checked against ground truth (spawns RC-36) | Corrected to +phi*d2/(2T), proven by an independent finite-difference of Black-Scholes delta as calendar time advances; locked in tests/test_charm_sign_finite_difference.py for BOTH bs_charm and compute_net_charm across ATM/ITM/OTM x 5d/20d. MEASURED on 600 real SPY/QQQ/IWM states: pre-fix sign-disagreed with the correct per-strike path on 70-79 percent, exact-negation ~12 percent; post-fix exact-negation 0 percent. END-TO-END: math_exposure_core.compute_net_charm producer -> call_engine time-decay note and features.fusion_model_input charm_direction feature consumers; decision vote already fenced at CHARM_VOTE_VALIDATION_STATUS=UNAPPROVED |
| RC-36 | CLOSED | 2026-07-26 | 2026-08-02 | Two charm engines (compute_net_charm scalar; math_levels.bs_charm / compute_charm_by_strike per-strike) existed with nothing asserting they AGREE, so RC-2's and RC-3's separate fixes left them mutual negatives | (1) two functions compute charm -> (2) written at different times for different callers, a scalar signal versus the charm walls -> (3) no single-source requirement covered DERIVED greeks, only vendor-field INPUTS (RC-38) -> (4) the single-source program was scoped to input parsing and never extended to derived computations -> (5) ROOT: single-source discipline stopped at vendor leaves and never reached derived greek math, so duplicate greek engines were free to drift | Unified the SIGN convention across both engines (both now +phi*d2/(2T) calendar) and locked their agreement with one shared finite-difference test. The residual near-expiry MAGNITUDE gap from differing time-to-expiry conventions is spawned as RC-37. END-TO-END: both charm producers now share one sign convention verified against one ground-truth derivative |
| RC-37 | OPEN | 2026-07-26 | 2026-08-09 | compute_net_charm and compute_charm_by_strike still differ in MAGNITUDE near expiry (~21 percent large-magnitude direction disagreement on 0DTE) because they use different time-to-expiry conventions | (1) the two agree on sign but not magnitude near expiry -> (2) compute_net_charm resolves T as hours-to-close for 0DTE while compute_charm_by_strike floors T at 0.5 days -> (3) charm_unit scales as 1/T so a tiny T explodes magnitude and can dominate the 0DTE-heavy aggregate -> (4) neither T convention has been proven the correct near-expiry choice -> (5) ROOT: there is no operator-approved canonical near-expiry T for charm, so two defensible conventions coexist | OPEN WITH A STATED REASON, not a punt: this MOVES the displayed charm magnitude, so it is an operator decision, not a silent pick. MEASURED: sign agrees post-RC-35; ~21 percent of 0DTE states disagree on direction purely from the T-convention magnitude. NEXT-DEPTH: measure flip magnitude under each T choice on the snapshot harness, present both to the operator, then single-source to the approved convention |
| RC-38 | CLOSED | 2026-07-26 | 2026-08-02 | Raw float()/int() coercion of Schwab vendor leaves across the money-path SILENTLY admitted NaN and infinity (float('nan') does not raise): NaN became a dict key, corrupted sorted strike sets (ATM and spacing), poisoned volume sums, entered the IV smile, produced a NaN spot past a <=0 guard, and passed abs(nan-target)>=0.01 as a FALSE contract match | (1) NaN reached strikes, volume and spot -> (2) raw float() admits NaN and a try/except of TypeError and ValueError does not catch it -> (3) each site coded its own inline coercion so there was no single reader to correct -> (4) the class was never seen as systemic because a try/except LOOKS safe -> (5) ROOT: numeric coercion of vendor leaves was open-coded with a non-obvious correctness hole and no enforced single reader | Routed every money-path vendor read through numeric_contract (float_finite_or_none / float_nonnegative_or_none) and added the ENFORCED gate tools/check_vendor_field_coercion.py catching both direct and intermediate forms, driven to zero across a 5-iteration self-adversarial sweep. The lock's field-list gap is spawned as RC-39. END-TO-END: producers server, math_exposure_core, math_levels, math_probabilities, market_state, market_context, order_flow_engine -> every downstream strike/volume/greek/level consumer; enforced so a regression fails the build |
| RC-39 | CLOSED | 2026-07-26 | 2026-08-02 | The vendor-coercion lock's VENDOR_FIELDS set was hand-maintained and PROVEN incomplete: 5 numeric price leaves (breakEven, extrinsicValue, high52Week, low52Week, percentChange) were absent, so the lock could not catch a raw float() on any of them | (1) the lock passed CLEAN yet a real float(underlyingPrice) NaN-spot bug existed -> (2) underlyingPrice was not in VENDOR_FIELDS -> (3) the field set was written from memory of the schema -> (4) nothing reconciled the list against the vendor's actual fields -> (5) ROOT: the authoritative vendor field set was not machine-readable to the gate, so the list was memory-derived (spawns RC-40) | Reconciled VENDOR_FIELDS against every numeric contract leaf in real captured chains, added the 5 missing leaves and an EXCLUDED_NUMERIC_LEAVES set for timestamps and ids. END-TO-END: tests/test_vendor_field_coercion_completeness.py derives numeric leaves from the snapshots DB and fails if any is neither covered nor excluded, so the schema and not memory is the authority |
| RC-40 | CLOSED | 2026-07-26 | 2026-08-02 | The authoritative Schwab option-contract field set existed only as a LIVE-PROBE tool (schwab_full_field_inventory.py, needs credentials), so no committed artifact enumerated the numeric leaves for a gate to read | (1) the gate could not import the vendor schema -> (2) the only inventory is a live API probe writing to an output dir -> (3) that probe cannot run in pre-commit or CI without credentials -> (4) so field coverage fell back to a hand list -> (5) ROOT: the vendor schema was captured only as an on-demand probe and never as a checked-in machine-readable set the build could reconcile against | Used the CAPTURED DATA as the standing authority: the completeness test derives the numeric-leaf set from real option_chain_json in the snapshots DB every run, so the committed data itself is the reconciliation source with no live credentials needed. END-TO-END: snapshots DB of captured vendor payloads -> completeness test -> VENDOR_FIELDS, drift fails the build |
