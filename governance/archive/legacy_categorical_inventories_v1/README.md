# Archived categorical section inventories (v1)

**Superseded:** Commit 1 restart (`TraceableDerivation` schema in `governance/traceable_derivation.py`).

These files used free-text `schwab_leaf` strings (e.g. `"upstream ms_dict / SignalInput"`). They are **not** valid closure evidence. Kept for diff reference during §A–§Q re-walk.

Active inventories will live as `governance/section_<letter>_traceable_inventory.py` after migration.
