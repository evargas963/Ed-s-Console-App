# Archived governance documents

RC-469 (operator right-sizing mandate, 2026-08-24). Every document here was measured as
INERT on 2026-08-24: referenced by no code, no test, no cursor rule, and no AGENTS.md law
(git grep over *.py, *.yaml, *.mdc, tools/, tests/, AGENTS.md). They are completed
contracts and registers kept for history, moved out of the live governance surface so the
live set stays the set that governs. Nothing here binds anything; git history preserves
every prior location.
