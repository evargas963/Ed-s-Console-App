# Generated: python re scan for ct.get( or c.get( in market_state.py
549:    return {k: ct.get(k) for k in keys}
555:        if (ct.get("putCall") or "").upper().strip() != side_up:
557:        sp = _f_ms(ct.get("strikePrice"))
676:    side_contracts = [c for c in contracts if (c.get("putCall") or "").upper() == side]
678:                      if _f_ms(c.get("strikePrice")) is not None})
799:        if str(ct.get("putCall", "")).upper().strip() != su:
801:        sp = ct.get("strikePrice")
810:            b = float(ct.get("bid")) if ct.get("bid") is not None else None
811:            a = float(ct.get("ask")) if ct.get("ask") is not None else None
830:        if str(ct.get("putCall", "")).upper().strip() != side_up:
833:            if abs(float(ct.get("strikePrice")) - strike_f) >= 0.01:
837:        raw_dte = ct.get("daysToExpiration")
