{
  "granted": true,
  "granted_at": "2026-08-06T00:00:00+00:00",
  "granted_by": "operator_delegation_in_session_2026-08-06",
  "scope": [
    "rc_status_vocabulary",
    "staged_lock_surface"
  ],
  "note": "Operator delegated governance authority in chat on 2026-08-06: 'governance approved, however we will review at a later time' and then 'i said you can run the governance'. Recorded as DELEGATED rather than self-granted so the attribution is honest and revocable: the agent did not decide this. Scope is deliberately narrow -- it authorises landing the single ENFORCED check rc_status_vocabulary (RC-257), which refuses an RC status token outside the declared set instead of silently skipping six close-contract clauses. Measured before granting: CLOSED gave 1+1 violations and blocked, while CLOSED_WITH_EVIDENCE, DONE, FINISHED and totally_closed each gave 0+0 and passed freely. The check returns 0 violations against the live 232-row ledger, so landing it blocks nothing that exists today. The second scope token, staged_lock_surface, is the one operating_process_lock actually tests -- operator_go_granted keys on it rather than on the check name -- and it is stated here explicitly rather than left as an undocumented side effect of the first. WIDER THAN INTENDED, disclosed rather than buried: staged_lock_surface authorises ANY staged enforcement-path commit, not only this check, because the lock offers no narrower token. Set granted to false as soon as this lands if that is not wanted. Revoke by setting granted to false."
}
