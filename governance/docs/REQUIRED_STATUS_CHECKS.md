# Required status checks (Phase 3D)

**Scope:** Institutional Audit Phase 3D — CI required-check specification; remote GitHub enforcement honestly marked unverified.

**Status:** CI configured locally | **Remote enforcement:** unverified

## Workflow

Primary workflow: `.github/workflows/objective-audit.yml`  
Required GitHub status check name: **`objective-audit`** (job name)

## Commands that must run in CI

| Check | Command |
|-------|---------|
| Objective audit | `python tools/enforce_all_rules.py --objective-audit` |
| Adversarial tests | `python -m pytest tests/adversarial/ -q` |
| Decision reconstruction | `python -m pytest tests/decision_reconstruction/ tests/release_object/ tests/test_governance_consolidation.py -q` |
| Agent preload tests | `python -m pytest tests/test_agent_preload_contract.py -q` |
| Agent preload checker | `python tools/check_agent_preload_contract.py` |
| Phase 3D checker tests | `tests/test_branch_protection_proof.py`, `test_required_status_checks.py`, `test_governance_critical_files.py`, `test_no_verify_resistance.py`, `test_governance_self_protection.py` |

## Artifact

`governance/artifacts/REQUIRED_STATUS_CHECKS.json` — generated by `tools/_build_institutional_audit_phase3d.py`

## Acceptable claim

**Required checks specified in CI workflow; GitHub enforcement not proven locally.**

Do not mark `remote_enforcement_verified: true` until branch protection on GitHub requires the `objective-audit` check.
