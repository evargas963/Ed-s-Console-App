"""Options / order-flow product ownership.

Live book math stays in ``order_flow_engine``. This package assembles the
UI-ready payload, chooses a default collectable contract from a banked chain,
and hydrates history from the one stream-capture store.
"""
