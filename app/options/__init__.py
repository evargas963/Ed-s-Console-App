"""Canonical options product ownership.

Contract selection lives in ``app.options.contracts``.
Order-flow payload and history live in ``app.options.order_flow``.
Book math stays in ``order_flow_engine``. This package does not open Schwab.
"""
