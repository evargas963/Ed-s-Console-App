{
  "_comment_guard_switches": "DISCOVERABILITY ONLY (RC-450 / Architecture A). These keys remain listed so the mandatory guards are visible. Setting a value to off/0/false does NOT disable the corresponding .py guard — subject-controlled env cannot authorize around a mandatory control. Emergency operator action lives outside the agent-controlled execution path and does not remove required CI/merge protection.",
  "env": {
    "ED_PRETOOLUSE_GUARD": "on",
    "ED_OPERATOR_LAW_GUARD": "on",
    "ED_PROCESS_LOCK_GUARD": "on",
    "ED_STOP_GUARD": "on",
    "ED_PROOF_ONLY_GUARD": "on",
    "ED_HONESTY_GUARD": "on"
  },
  "_comment_hook_chains": "WIRED (SIMPLICITY REHAB, operator GO 2026-08-24): tools/stop_chain.py and tools/pretooluse_chain.py run the SAME guard modules in ONE process each — measured 300ms per event vs 2,358ms (Stop, four interpreters after RC-471; 2,815ms for the pre-RC-471 five-member chain) and 1,704ms (Edit chain, three interpreters). The roster stays visible as argv on each command line, so this file remains the reviewable authority over exactly which guards run; a block from any member still blocks, and a crashing member blocks (never passes). .cursor/hooks.json mirrors this wiring.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": ".venv/Scripts/python.exe tools/pretooluse_chain.py tools/pretooluse_guard.py tools/operator_law_guard.py tools/process_lock_guard.py"
          }
        ]
      },
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": ".venv/Scripts/python.exe tools/pretooluse_chain.py tools/operator_law_guard.py tools/process_lock_guard.py"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": ".venv/Scripts/python.exe tools/stop_chain.py tools/stop_guard.py tools/proof_only_guard.py tools/honesty_guard.py tools/operator_law_guard.py"
          }
        ]
      }
    ]
  },
  "_comment_rc471_stop_dereg": "RC-471 (operator order 2026-08-24): process_lock_guard.py removed from Stop ONLY. It still runs on every PreToolUse Edit/Write AND Bash/PowerShell call (both registrations above). Its Stop-only rails (index-parity scan, LIVE-vs-DISK, completion-claim) no longer run in-session — index parity still binds at commit via the operating-process pre-commit hook; the LIVE-vs-DISK and completion-claim Stop rails were retired with the dereg. The remaining Stop guards (stop_guard RC-72, proof_only, honesty, operator_law) are unchanged.",
  "_comment_process_lock_guard": "RC-217 (operator 2026-08-03): the day's failures were PROCESS failures — two agents writing one tree, claims measured an hour before they were made, a killed pre-commit stripping the worktree, LIVE claimed while the running PID predated the commit. governance/AGENT_OPERATING_PROCESS_V1.md is the checklist; tools/operating_process_lock.py is the mechanism; tools/process_lock_guard.py fires it on PreToolUse for Edit/Write AND Bash/PowerShell (Edit/Write: the RC-442/RC-477 cross-checkout rail protecting the primary working tree; Bash/PowerShell: destructive-git and piped-commit rails; index parity binds at commit via the operating-process pre-commit hook; the role/GO/mission rails were removed in the 2026-08-24 Architecture A teardown). RC-471: its Stop registration was removed.",
  "_comment_enforcement": "HISTORY (2026-07-16, SLIMMING 1B): the original Stop hook (python tools/enforce_all_rules.py --stop-hook) was retired with the enforcement stack it ran. That note described July 2026 and is NOT the current state — the Stop chain registered above (stop_guard, proof_only_guard, honesty_guard, operator_law_guard) has run since RC-72/RC-87/RC-209/RC-93 and was chained into one process on 2026-08-24. Current authorities: the guard chains in this file (in session), the pre-commit fast gates in .pre-commit-config.yaml (at commit), and required CI — pytest-full + hardening, which runs the enforced catalog and the delta gate — at merge (RC-406).",
  "_comment_pretooluse_guard": "RC-470 (operator right-sizing 2026-08-24): the RC-66 edit-time lane (a root-cause row demanded before editing any production file) is RETIRED with its commit-time twin - governance/retired_checks.md; feature-branch edits are autonomous. What tools/pretooluse_guard.py still BLOCKS: RC-160 (operator 2026-07-30) Write/Edit of prompt/agent-instruction paths whose new content frames SPY-only / sentinel-complete work without UNIVERSAL or OUT-OF-SCOPE language — including under reports/ and .claude/ — via tools/universal_scope_lock.py; RC-163 (operator 2026-07-30) residual/handoff/RC/prompt Writes whose new content claims Collect/Chart Done while Chart render stays soft OUT-OF-SCOPE/OBSERVED, or schedules live proof with a hardcoded weekday name when next RTH (America/New_York + is_trading_day_et) is a different weekday — via tools/chart_intent_lock.py; RC-186 unapproved UI redesigns on gated surfaces. RC-450 (Architecture A): the ED_PRETOOLUSE_GUARD env switch is inert — it cannot disable this control.",
  "_comment_stop_guard": "RC-72 (operator 2026-07-27): every other lock fires on an ACTION; the agent's worst failure is the ABSENCE of one - writing a tidy summary ending in 'next: X' and stopping. No PreToolUse hook can catch that because no tool is called. Observed repeatedly on 2026-07-27, including immediately after the agent wrote 'I'm not stopping here. Building the Stop hook now.' and then stopped. tools/stop_guard.py BLOCKS (exit 2) the end of a turn when a root-cause row opened THAT DAY is still OPEN with a fix cell reading IN PROGRESS / VERIFICATION PENDING / NOT FIXED / PARTIALLY FIXED. It honours stop_hook_active so it can never hang a turn, and ignores rows from earlier days (those are a dated backlog that check_root_cause_log already enforces). RC-450 (Architecture A): the ED_STOP_GUARD env switch is inert — it cannot disable this control.",
  "_comment_proof_only_guard": "RC-87 (operator 2026-07-27, non-negotiable, 'no memory ever cited. only proof.'): every other lock fires on an ARTIFACT - a tool call, a file, a commit. The agent's most expensive failure has none: it is a SENTENCE. 'GEX-R1 RETIRED BY MEASUREMENT' was cited as settled fact across several turns; when finally re-derived the study had n=66, a 95% CI of [-0.289, +0.194] which CONTAINS the founding -0.22 it was used to reject, and 43% power to detect that effect - a coin flip recorded as a kill, propagating for days because a memory line said RETIRED. tools/proof_only_guard.py runs as a Stop hook, reads the turn's final assistant message from transcript_path, and BLOCKS (exit 2) when memory/prior conclusions are cited as the ground of a claim, or when a hard verdict (KILL/RETIRED/PROVEN/CONFIRMED/DISPROVEN/does-not-replicate) is stated without SAME-TURN proof - a runnable command in backticks, or n= beside a CI/power. Quoting memory in order to CORRECT or VOID it is explicitly allowed: that is the cure. It honours stop_hook_active so it can never hang a turn, and reports an unreadable transcript as a violation rather than a pass (RC-57: unmeasurable is never compliant). RC-450 (Architecture A): the ED_PROOF_ONLY_GUARD env switch is inert — it cannot disable this control.",
  "_comment_operator_law_guard": "RC-93 (operator 2026-07-27): 'the locks you have on your .claude/settings.json are for shit. you haven't captured half of what i have told you.' Correct — three hooks covered root-cause rows, unfinished turns and memory-as-evidence, while every other standing law was enforced by the agent choosing to remember it, which is the actor with the measured failure rate (24 errors logged in one session, 29 percent caught by the operator). tools/operator_law_guard.py encodes THE NUMBER ONE LAW as stated this turn — 'you do not assume and you do not act from memory or recall or any other word that is used to indicate that you used something less than proof' — plus the no-grep law (2026-05-22) and a ban on disabling any lock. It registers at THREE points because a law that only fires at the end fires after the damage: PreToolUse(Bash|PowerShell) BLOCKS the action (grep/rg against repo files, destructive git, --no-verify or any ED_*_GUARD=off attempt); PreToolUse(Edit/Write) BLOCKS closing an RC row with no same-turn verification; Stop BLOCKS ending a turn that edited production code and executed nothing. The RC-93 rewrite inspects no prose — the banned end-of-turn phrases (2026-05-27) are not checked here; tools/honesty_guard.py carries a narrow end-anchored check ('Want me to…?'/'Should I…?'/'Shall I…?'), and the general menu norm is operator review. Filtering a command's OWN stdout is an explicit carve-out, stated rather than assumed, because on 2026-07-27 the agent used it all session and could not say whether it was permitted (error E-22). 13 negative controls prove it blocks and permits correctly. RC-450 (Architecture A): the ED_OPERATOR_LAW_GUARD env switch is inert — it cannot disable this control, and --no-verify has no grant path."
}
