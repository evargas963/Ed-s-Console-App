# Credentials & secrets
schwab_token.json
*.json.bak
.env
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Node
node_modules/

# Logs & temp files
*.log
diagnostics.txt
report.txt
login_err*.txt
login_out*.txt
manual_err.txt
manual_out.txt
reauth_capture_*.txt
feature_contract_validation.txt

# Data files (large/sensitive)
data/*
!data/trading_calendar/
!data/trading_calendar/us_equities.json

# Schwab field inventory — track curated canonical references only
schwab_field_inventory/**
!schwab_field_inventory/
!schwab_field_inventory/README.md
!schwab_field_inventory/schwab_canonical_fields.txt
!schwab_field_inventory/schwab_field_dictionary.csv
!schwab_field_inventory/schwab_field_dictionary_grouped.csv

# Test artifacts
test-results/

# Generated daily health reports
reports/daily_health/history/*.json
reports/daily_health/history/*.md
reports/daily_health/latest_daily_health.*

# OS
.DS_Store
Thumbs.db

# Model artifacts — local runtime outputs (not source control)
# Legacy baseline tickers may remain tracked in git history; new scheduler/training
# outputs under models/active* are local runtime only — see governance/docs/SOURCE_CONTROL_HYGIENE.md
models/parallel/
models/cascade/
models/_artifact_archive/
models/cache/
models/training_report.jsonl
models/arch_state*.json
models/arch_eval_proof.json
models/eval_dashboard_metrics.json
models/realized_contract_eval_aggregate.json
models/arch_competition/
models/active/
models/active_5c/
models/active_15c/
models/active_60c/

# Benchmark and runtime logs
benchmark_logs/
.runtime/

# Tooling caches / local agent (machine-specific; do not commit)
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Host manifest snapshots (regenerate via scripts/export_host_manifest.ps1)
host_manifest.json
host_manifest.*.json

# Cursor IDE — track project rules only
.cursor/*
!.cursor/rules/
!.cursor/rules/**

# Full-repo Schwab scanner dumps (multi-GB; keep partial register in git or store offline)
governance/SCHWAB_UNIVERSAL_COVERAGE_REGISTER_V4.mock_build.csv
# V4 register CSV — generated locally / in CI; audit pins in schwab_v4_register_build_meta.json
governance/SCHWAB_UNIVERSAL_COVERAGE_REGISTER_V4.csv

# Operator/tool dry-run audits (local only)
governance/audits/backfill_et_clock_from_ts_utc_v1_*.json

# Local auth / secrets (Schwab OAuth redirect capture — never commit)
_schwab_auth_url.txt

# Local databases and backups (operator snapshots — not source)
backups/db/*
!backups/db/.gitkeep
calibration/trading_data.db

# Logs and local scoreboard reports
logs/
reports/daily_scoreboard/

# Local analysis / audit scratch outputs (repo root)
feature_ablation_universe.xlsx
feature_analysis.xlsx
feature_consolidated_full.xlsx
enforce_all_out.txt

# Local timing probes (repo-root scratch — not production tooling)
timing_probe.py
timing_probe2.py

# Static design mockups (not wired to production; local review only)
static/redesign_mockup.html

# Remaining model paths (candidates, archives, caches, run outputs) — not source
models/**
# GOV-GATE-PERF-V1: cross-process governance gate cache (success-only, full-identity keys)
governance/artifacts/.gate_cache/

# EXEC_IDENTITY noncanonical runtime-proof scratch databases (local evidence only)
data_proof/
